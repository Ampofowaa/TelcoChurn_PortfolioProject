# CLAUDE.md

## Project Overview

End-to-end ML portfolio project on the IBM Telco Customer Churn dataset. The goal is a production-grade system covering data ingestion → validation → feature engineering → model training → serving → monitoring. The full build plan is in `PROJECT_PLAN.md` (15 phases, 0–14, ordered as a natural data-science lifecycle). Always consult it before starting work on a new phase or feature.

## Source of Truth

- **Modelling science:** `notebooks/_archive/EDA-original.ipynb` is a **comparison notebook, not a source of truth** — the original exploratory pass over data validation gates, preprocessing logic, feature engineering decisions, Optuna hyperparameters, calibration, cost-sensitive threshold derivation, and evaluation metrics. `src/` and `ANALYSIS.md` are authoritative wherever they diverge from it; the archive is useful only as a historical reference for *why* a decision changed, not as a bar current work must clear. Significant deviations (wider search ranges, different selection rules, different decision mechanisms entirely, etc.) are expected at this point in the project — document each new decision's rationale in `ANALYSIS.md`, or inline (config comments, docstring, commit/PR body) for smaller ones, rather than re-justifying current work against the archived notebook.
- **Modelling rationale:** `ANALYSIS.md` documents every modelling decision, analytical results and findings, and known limitations — the full narrative from data insights through to model results. **§0 (Problem Framing & Cost Definition)** is the entry point — it defines the prediction unit, label definition, cost structure, success criteria, and the two modelling invariants (test set touched once; one metric drives selection) that govern all phases. Do not contradict it.
- **System & workflow diagrams:** `docs/architecture.md` — system architecture diagram (infrastructure flow) and ML workflow diagram (modelling lifecycle with the two feedback loops).
- **Project landing page:** `README.md` is the recruiter-facing overview (headline metrics, pipeline diagram, quick start, project status). It does not contain modelling rationale.
- **Build roadmap:** `PROJECT_PLAN.md` defines the 15-phase lifecycle-ordered execution plan. Do not implement features that belong to a later phase.

## Key Commands

These commands are set up in Phase 0. Run them from the project root.

```bash
uv sync                          # install / sync dependencies
uv run pytest                    # run test suite
uv run pytest -k "test_name"     # run a single test by name
uv run ruff check src/           # lint
uv run mypy src/                 # type-check (strict on src/ only)
uv run pre-commit run --all-files  # run all pre-commit hooks

dvc repro                        # re-run only changed pipeline stages
dvc repro train                  # re-run a single stage (and anything it invalidates downstream)
dvc repro --force                # re-run all stages regardless of cache
dvc dag                          # show the pipeline DAG

mlflow ui                        # open experiment tracking UI (localhost:5000)

make format                      # ruff format src/ then black src/ — always in this order
make lint                        # shortcut: ruff check + mypy
make test                        # shortcut: pytest --cov=src
make calibrate                   # shortcut: uv run python -m telco_churn.models.calibrate (optional RUN_ID= override)

docker compose --profile infra up -d    # start Postgres + MLflow (Phase 1+ / Phase 5+)
docker compose up -d                    # start full local stack (Phase 9+)
```

## MLflow Model Registry

- **Tracking backend:** **Postgres**, via the MLflow server (`docker-compose.yml`: `--backend-store-uri postgresql://…@postgres:5432/mlflow`). `mlruns/` is the **artifact** store only (`--artifacts-destination`), not the backend — do not describe it as the tracking backend. Phase 12 swaps Postgres for RDS and the artifact root for S3: a URI change, not a migration off files.
  - **The fallback must stay a database backend.** `configs/config.yaml` resolves `tracking_uri: ${oc.env:MLFLOW_TRACKING_URI,sqlite:///mlflow.db}`; without `.env` or the infra profile (fresh clone, CI) that SQLite default is what runs. **Never default it back to a bare path like `mlruns`** — that is MLflow's file store, which as of 3.14 is in maintenance mode, **raises on use**, and does not receive new features (including the `LoggedModel` entities the evaluation logging depends on). SQLite is not an alternative to Postgres; it is the zero-infra fallback with the same SQLAlchemy feature surface, so local/CI never diverges from served mode.
- **Registered model name:** `telco-churn-pipeline`
- **Production alias:** `champion` — the FastAPI service loads whichever run carries this alias at startup.
- **Challenger alias:** `challenger` — used during evaluation before promotion.

### Run artifacts vs. registry versions

The **run** is the experiment record; the **registry** is the deployment surface. Everything fitted during a training cycle is logged to the run and is retrievable there via `runs:/<run_id>/model` — nothing needs registering to be loadable. A registered model version answers exactly one question: *could this be serving traffic right now?*

- **Model-version tag `promotion_status: pending | promoted | rejected`, set to `pending` at the moment the version is minted.** A version is a valid rollback target **iff** it carries `promoted`. This is the standard registry approval-state field — SageMaker ships it as `ModelApprovalStatus`, and it is exactly what MLflow's own docs prescribe to replace the deprecated `stages` (aliases for the deployment pointer, tags for the version's state). The registry is *supposed* to hold unapproved candidates: CI needs a stable artifact reference to validate against, the audit trail wants failed candidates recorded rather than vanished, and Phase 10's Prefect flow passes a model URI between steps. **Registering an unvalidated candidate is normal; leaving it unlabelled is the defect.**
  - **`pending` is the mint-time default because the safe state must be the one you get for free.** A `rejected` tag written only on the failure path is a tag that the next abort path will forget to write — and a crash, an OOM, or an unhandled exception writes nothing at all. Defaulting to `pending` makes every one of those failure modes fail safe: you never have to remember to mark something *bad*, only to mark something *good*, and that step is already load-bearing.
  - **The fail-safe default produces residue by design, and that residue has a defined drain: stale `pending` versions are reaped.** The same property that makes `pending` safe — a crash leaves it behind — means every crashed evaluation, OOM, or unhandled exception deposits a version stuck at `pending` forever. `pending` is meant to be *transient*: a version resolves to `promoted` or `rejected` within its own cycle, so **a `pending` version older than one training cycle is definitionally a crash artifact**, carrying no rollback value (not `promoted`) and no audit value (never decided). Those — and only those — may be pruned. This is *retention hygiene, not size management*: `promoted` versions are the rollback surface and are kept unconditionally; `rejected` and `dev`-scope versions are the audit trail and are kept; **the reap touches `pending` orphans exclusively.** The point is rollback *legibility*, not disk: unreaped orphans lengthen and pollute the version list a human scans under pressure, which is the exact hazard the tag-based rollback rule below exists to prevent — so leaving them to accumulate quietly erodes that rule. No automated reaper is required for this project; a manual prune utility (surfaced in the Phase 14 runbook) is the intended scope, and it must filter on *both* `promotion_status == pending` **and** an age threshold, never on version number.
  - **Emergency rollback selects on the tag, never on version arithmetic.** `set_registered_model_alias(name, "champion", version=N-1)` — *"the previous number"* — is the command that goes wrong: a rejected or crashed evaluation cycle leaves a `challenger`-registered version with no `champion` alias and no `promotion_status: promoted` tag, which is **indistinguishable in a version list from a legitimate previous champion** if you go by version number alone — both are just "some earlier integer." The command then succeeds, the service restarts cleanly, and it silently serves a model that failed its gate. Roll back to the highest version tagged `promotion_status: promoted`.
- **Validate, then promote — never promote, then validate.** The alias flip is the moment traffic moves; nothing unverified may sit behind it. Load the candidate by **explicit version URI** and run the full smoke check (schema, output shape/range, golden-prediction parity) *before* `set_registered_model_alias`. Flip only on a pass, then re-assert parity **through the alias** as a thin confirmation the pointer landed — and **roll the alias back if that fails**. Flipping first and checking afterwards is a Kubernetes readiness probe that runs after the pod is already taking traffic; it leaves `champion` pointing at a model that failed its serving check, and Phase 9's FastAPI loads `models:/telco-churn-pipeline@champion` at startup.
- **One registration per training cycle, at the point the artifact becomes serving-shaped.** Calibration is part of the model's inference graph, not a downstream consumer of it — `CalibratedClassifierCV(Pipeline([preprocessor, lgbm]))` is one sklearn estimator with one `predict_proba`, logged with one `log_model` call. The uncalibrated pipeline is a *stage of construction*, not a model that lost a comparison: it is logged to the run for auditability and **never registered**.
- Concretely: **Phase 5 logs, does not register** (no `registered_model_name=`, no alias). **Phase 6 `calibrate.py` fits and logs the calibrated pipeline — a pure, deterministic file transform — and performs no registry write of its own**, ever, from its own process. `register.py` is the single entry point for every registry write in the cycle, run as its own CLI step (`python -m telco_churn.models.register`) *after* `calibrate.py` has already exited, never called from inside it: given `register.run_id` (or defaulting to `calibrate.py`'s own `reports/calibrate_receipt.json` when omitted), `register_challenger` mints the version, tags it `promotion_status: pending` at mint time, and points `challenger` at it; given `register.model_version` instead (an already-registered version — the two are mutually exclusive on the same CLI, dispatched by which one is set), `run_registration_step` runs the promote/reject path described next. **Phase 7 `evaluate.py`/`error_analysis.py` → `register.py`** then closes the cycle — `evaluate.py`/`error_analysis.py` write no model-version tags either (see the tag-ownership rule below); `register.py` reads their `reports/eval_receipt.json`/`reports/error_analysis_receipt.json` bootstrap pointers, tags the model version, checks the human review verdict, and either flips `champion` onto the version it minted, or leaves it rejected. An alias is a unique pointer per model name — re-aliasing does not create a second challenger, it silently orphans the previous version.
- **The gate measures the exact artifact it promotes.** The promotion decision is computed on the registered version's sealed-test metrics, and a pass flips `champion` onto that same version — there is no second fit and no second artifact standing in for the one that was measured.
- **The decision precedes the action, structurally.** The gate lives in `models/gate.py` as a **pure function** (`decide_promotion`), is called by `evaluate.py` at the moment the sealed-test metrics exist, and is logged as `promotion_decision.json` onto `evaluate.py`'s own `evaluation` run — carrying the regime, the per-criterion breakdown, the evaluated model version, and a `metrics_content_hash` stamp. It carries no `review` field: the human verdict is a separate, append-only document, **`promotion_review.json`**, logged onto the same eval run by `models/review.py` — a standalone CLI, independent of Jupyter, and the sole entry point that writes it (notebook 05 displays the diagnostics a reviewer needs but does not itself stamp a verdict) — via `gate.py::record_review` — `{eval_run_id, metrics_content_hash, entries: [{verdict, notes, approver, reviewed_at}, ...]}`. Append-only because `log_dict` to a fixed artifact path overwrites: a `rejected` stamp later overridden by `approved` must not vanish without trace. `register.py` **reads** `promotion_decision.json` and `promotion_review.json["entries"][-1]` rather than recomputing or re-deriving either, aborting if `promotion_decision.json`'s embedded model version does not match the version being registered. A human `rejected` verdict is routed through the same `_tag_rejected` path as a gate failure — left at `pending` it would be indistinguishable from a crash artifact. Two independent evaluations of the same rule are how the rule becomes two rules.
- **`register.py` resolves every cross-cycle artifact by tag, never by a local `reports/` path — and it is the only module that writes a model-version tag.** `eval_run_id`, the four gate criteria, and `error_analysis_run_id` are tagged onto the model version by `register.py` itself (not by `evaluate.py`/`error_analysis.py`, which write no registry tags at all), resolved from the tag if a prior invocation already wrote it, else from `reports/eval_receipt.json`/`reports/error_analysis_receipt.json` — the same first-invocation bootstrap role `reports/calibrate_receipt.json` already plays for `threshold.py`/`evaluate.py`/`error_analysis.py`'s own input resolution — verified against the model version and tagged immediately, so every later read goes through the tag and never the receipt again. This mirrors `calibrate.py`'s `logged_model_id` tag, since `ModelVersion.model_id` doesn't auto-populate in OSS MLflow 3.14 and nothing else creates a version-to-run link. `register.py` reads `promotion_decision.json`/`metrics.json`/`economics.json`/`test_predictions.parquet` via `runs:/<eval_run_id>/...` and `error_analysis.json` via `runs:/<error_analysis_run_id>/...`, cross-checking each against the `model_version` being registered (a `metrics_content_hash` stamp for the former, a `model_version` field for the latter). `reports/` is still written by every step as a human-inspection mirror, but beyond the receipts' one-time bootstrap role `register.py` never reads it back — a fixed local path reflects whichever run last executed `evaluate.py`/`error_analysis.py` on this machine, not necessarily this model_version's own cycle, the same staleness failure mode the `drift_reference.json` rule below exists to prevent. **A refactor back to a local `reports/` read is a correctness regression, not a simplification** — same footing as `drift_reference.json`'s champion-alias rule and `evaluate.py`'s resolve-by-`run_id` rule.

- **Logged artifacts per run (every training cycle, Phase 5+):** model (pyfunc), `feature_space.txt`, `feature_columns.txt`, `preprocessing.pkl`, `training_manifest.json`.
  - `feature_space.txt` — full **feature space**: every column `build_feature_df` produced before selection (the complete output of the Phase 4 feature pipeline).
  - `feature_columns.txt` — **model input space**: the subset that survived `select.py` and entered the `ColumnTransformer`. The diff between `feature_space.txt` and `feature_columns.txt` is what selection dropped for that run — a per-run audit trail that requires no git lookup.
  - `preprocessing.pkl` — fitted `ColumnTransformer`; encodes the exact transformations applied to the model input space at training time.
  - `training_manifest.json` — **engineering audit trail**: git SHA, `data_content_hash` (sha256 of the processed features parquet, via `features/accessor.py::features_sha256()` — independent of DVC, since pipeline-stage outputs are tracked in `dvc.lock`, not a per-file `.dvc` sidecar, so no such sidecar for this file will ever exist to hash), model family, full hyperparameters, `feature_space`/`feature_columns`, CV PR-AUC, a `model_family_committed` section (`model_family`, `decision_reference: "ANALYSIS.md §4a"`, `decision_run_id` — the frozen model-family decision referenced via `models/train/common.py::COMMITTED_MODEL_FAMILY`/`COMMITTED_MODEL_FAMILY_DECISION_RUN_ID`, never recomputed live — `candidates.py`/`comparison.py` run only from `notebooks/03a-model-selection.ipynb`'s on-demand review, not the automated pipeline), `logged_model_uri` **and `logged_model_id`** (the MLflow 3 `LoggedModel` id — metrics attach to a *(model, dataset)* pair via `log_metric(..., model_id=..., dataset=...)`, and `ModelVersion.model_id` does **not** auto-populate in OSS MLflow, so this id must be persisted deliberately or the registry cannot reach the model it points at), and `tuning_summary` (trial counts, selected vs. raw-best trial number/score, 1-SE standard error and band floor — the diagnostics behind the `selection_rule` pick).
- **Persist the evidence, not just the conclusion.** Any module whose output rests on a computed array must **log that array**, not only the summary or the chart derived from it. Re-derivation is only sound while the dataset is static and the folds are seeded; under retraining it silently reconstructs *different* numbers than the ones that cycle's decisions rested on, and nothing flags it. Concretely:
  - `calibrate.py` selects the calibration method, reports BSS/ECE, and validates `t*` from the dev-OOF probability vector — so it logs **`dev_oof_predictions.parquet`** alongside `calibration_summary.json`. Downstream (the drift reference's score baseline, the dev-OOF veto surface, the calibration screen) then reads the exact numbers the decision was made on.
  - `error_analysis.py` renders SHAP charts — so it logs **`shap_values.parquet`**, not only the figures. A rendered plot answers the question you thought to ask; the values answer the one you did not.
- **Evaluation (Phase 7 `evaluate.py`) gets its own `evaluation` run, artifacts foldered, metrics namespaced `test_*`.**
  - **Its own run — a sibling of `model_comparison` / `tuning_study`**, following the step-per-run pattern. Not appended to the `dev` version's run: that run already holds Phase 5's pipeline + manifest and Phase 6's calibrated model + calibration summary, and Phase 10 needs each cycle's evaluation runs to be comparable peers in the UI.
  - **Linkage is native — this project runs MLflow 3.14.** A logged model is a first-class `LoggedModel` with its own `model_id`, and **metrics attach to a (model, dataset) pair**: `mlflow.log_metric(..., model_id=..., dataset=...)`. Log every evaluation metric with both. The metrics then belong to the *model*, not to whichever run computed them — so a separate evaluation run stays fully attributable. **Do not hand-roll this with tags.** Consequence: Phase 5/6 must record `logged_model_id` (from `log_model`'s returned `ModelInfo`) in `training_manifest.json`, or Phase 7 has nothing to log against.
  - **The CV-vs-test collision is structural, and the dataset dimension is what resolves it.** Phase 5's `pr_auc` (dev, cross-validated) and Phase 7's `pr_auc` (sealed test) are different quantities that must never be conflated. Keyed by dataset, they are distinct rows. Keep a `test_` prefix as well — not for correctness now, but because metric keys surface in search/exports where the dataset column is not shown.
  - **The four gate criteria are also set as model-version tags** (`test_pr_auc`, `test_recall`, `test_brier`, `test_calibration_slope`), by `register.py`, on **every** version it processes — promoted or rejected, not only the ones that end up serving. `LoggedModel` metrics answer *"what did this model score?"*; the **registry** is where a rollback is chosen, under pressure, by someone scanning a version list — and this is exactly where those numbers must be legible without opening a run, including for a rejected candidate a reviewer is trying to understand.
  - Artifacts sit at the run's root (`metrics.json`, `economics.json`, `promotion_decision.json`, the prediction parquets), with sub-paths only where they separate different *kinds* of output — `figures/`, `slices/`. Do not prefix them `evaluation/`: the run already is the evaluation. Sliced/fairness detail is an **artifact**, not hundreds of metric keys — except `test_equal_opportunity_diff` and `test_demographic_parity_diff`, which gate §0's V2 and should be plottable across cycles.
  - **Business metrics are metrics — but they are not model properties, and the cost inputs must be logged alongside them or they are uninterpretable.** Expected value, campaign cost, retained revenue, contacted count, and break-even retention rate are logged as MLflow metrics per scenario (plus the `treat-all` / `treat-none` baselines), because Phase 10 needs them plottable across cycles. **But `test_ev_base` moves when the model changes *or* when `configs/costs.yaml` changes, and in the UI those causes are indistinguishable.** So log the cost inputs as **run params** and set a **`costs_config_hash` tag**: same hash ⇒ the model changed; different hash ⇒ the assumptions did. The supporting arrays (EV-vs-K curve, sensitivity sweeps, break-even grid) go to `economics.json`, not metric keys. **EV is never a model-version tag** — those carry exactly the four gate criteria, and that set is legible precisely because it is exactly the criteria; §0 makes EV a diagnostic, so tagging it onto the version invites a rollback decision on a number no gate ever admitted.
- **`drift_reference.json` — written once, at champion promotion (Phase 7 `register.py`).** The baseline every Phase 10/13 drift check measures against: per-feature reference distributions, the reference prediction-score distribution, and the reference churn prevalence. Two rules, both mandatory:
  - **MUST be logged as an MLflow artifact on the promoted run, and MUST be read back by resolving the `champion` alias.** Never `reports/drift_reference.json` as the source of truth — a copy may be mirrored there for human inspection, but no code may *read* it. The reference travels with the model version or the system lies: an emergency rollback re-points `champion` at version N−1, and a fixed-path reference does not roll back with it, so every subsequent drift check compares live traffic to the *previous* champion's score distribution and reports drift that is really the model swap you just performed. The prediction-score half of the reference is genuinely model-specific (v3 and v4 score customers differently); the feature half is closer to a data property and would survive the mismatch — it is the score half that produces the phantom alerts. `register.py` writes it via `mlflow.log_artifact` onto the promoted run; `pipelines/drift_check.py` fetches it from the `champion`-aliased run. **A refactor that reads the reference from a fixed path is a correctness regression, not a simplification** — same footing as `evaluate.py`'s resolve-by-`run_id` rule.
  - **The score baseline MUST be built from out-of-sample probabilities** (dev-OOF ∪ sealed-test — all 7,043 rows, each scored by a model that never trained on it), *never* from the champion scoring its own training data. In-sample scores are systematically sharper, and baselining on them produces a permanent phantom PSI signal against honest production scores that no amount of investigation will resolve. Feature distributions have no such problem and come from the champion's actual training data.
- **`model_card.json` — written once, at champion promotion (Phase 7 `register.py`), not at Phase 6 challenger registration.** Stakeholder-facing narrative: intended use, known limitations, performance summary **vs. the decision-relevant baselines**, fixed-recall profile, fairness/robustness flags. **Those baselines are `treat-none` / `treat-all` and the `DummyClassifier` prevalence floor — never LogReg.** LogReg was a candidate in Phase 5's *family comparison*, not a rival to the champion; a stakeholder needs to know what the model buys over doing nothing and over the naive policy, not which algorithms lost. Nor should it be re-scored on the sealed test set to fill this line: the family decision rests on 100 paired CV folds, and a single 1,409-row holdout would yield a *noisier* Δ that reads as *more* authoritative for being a "test-set number" — a straight downgrade. The family comparison belongs in `ANALYSIS.md` §4 and `training_manifest.json`'s paired-Δ, which already carry it. It is not populated when `register.py` mints the challenger — at that point the model is calibrated but un-thresholded and untested on the sealed set, so a "known limitations" section would still be a disclaimer about an unfinished product. No hyperparameters, hashes, or raw statistical detail either way — that lives in `training_manifest.json`. **Assembled from artifacts, never hand-transcribed:** performance and the fixed-recall profile come from `metrics.json` (resolved via the model version's `eval_run_id` tag, tagged by `register.py`); the fairness/robustness flags and known limitations come from `error_analysis.json`, computed by **`models/error_analysis.py`**, not the notebook — it is a pipeline artifact, and Phase 10's automated retrain has no human to run Jupyter — and resolved via `error_analysis_run_id`, tagged onto the model version by `register.py` (from `reports/error_analysis_receipt.json` on its first pass for this version, from the tag itself thereafter). `register.py` **aborts if it cannot resolve an `error_analysis_run_id` for the version, or if that run has no `error_analysis.json`** — a fairness section retyped from a rendered notebook cell is exactly the section that gets quoted back at you, and it must be reproducible from the artifact that produced it. `business_case.expected_impact.capacity_budget_check` is read the same way, off `economics.json` on the same `eval_run_id` (no new resolution — `register.py` already loads this artifact for `ev_by_k_logged`), and is `None` rather than aborting when the eval run predates it or otherwise has no `economics.json`, since a capacity/budget breach is a diagnostic (§0: EV and its supporting economics never gate) and unlike the fairness/limitations fields above has no promotion decision riding on it.

## Data Handling

- **`datasets/raw/` is read-only.** Never modify, overwrite, or delete any file there.
- **The raw CSV is committed to Git; DVC owns only derived artifacts.** DVC lists `datasets/raw/*.csv` as a stage **`dep`** (hashed into `dvc.lock`, re-runs the DAG on change), not an **`out`** (cached, gitignored, remote-backed). A dep may be any path in the repo. The file is 977 KB, static, and read-only — none of DVC's reasons to own raw data apply, and gitignoring it would break fresh-clone reproducibility and Phase 11's CI. `datasets/processed/` is gitignored; DVC owns it as stage output.
- **No DVC remote is required before Phase 12.** A local cache is the full configuration. Phase 12 *adds* an S3 remote for shared caching; it is not a migration, and nothing depends on it.
- **Postgres is a materialised cache of the CSV, never a source of truth.** Its contents are fully determined by the raw CSV + `sql/schema/` + `data/ingest.py`. It cannot be a DVC `out` (outs are files); the `ingest` stage emits `reports/ingest_receipt.json` instead. A hand-edited `customers_raw` is invisible to `dvc repro` — rebuild with `dvc repro --force`.
- **`datasets/processed/telco_churn_features.parquet`** is the processed-features artifact, written by `features/build.py`'s `__main__` (`dvc repro features`) — the DVC `features` stage's declared, cached output. Read it through `features/accessor.py::load_features()`, never by constructing the path inline. `cfg.paths.processed_data` itself is readable only by `features/accessor.py` and `data/split.py` — enforced by `tests/unit/test_architecture.py::test_processed_data_path_is_read_only_by_its_canonical_resolvers`.
- **TotalCharges gotcha:** the raw CSV has `TotalCharges` as a string with whitespace for 11 zero-tenure customers. Coerce to numeric first, then impute with median — never drop.
- **Target column** is always named `churn` (binary: 0/1).
- **Class imbalance handling is required** — never train a model without it.
- **Random state is always 42** for reproducibility across all splits, samplers, and models. No `src/` call site hardcodes the literal — each stage reads its own `random_state`/`random_seed` key from its own config file (`configs/config.yaml`'s top-level `random_seed` for the dev/test split and CV folds; each of `configs/{calibration,threshold,evaluate,training/feature_selection}/default.yaml`'s own `random_state` for its stage), deliberately kept as separate keys rather than one shared or interpolated value — DVC hashes each stage's `params:` independently, so unifying them would either rerun the whole DAG on any single-stage seed edit or make the dev/test split reachable from what should be an isolated per-stage change. A hardcoded literal other than `42` is still rejected — enforced by `tests/unit/test_architecture.py::test_random_state_literals_are_always_42`.

## DVC Pipeline

`dvc.yaml` wraps the straight-line, file-producing half of the modelling lifecycle — `ingest → validate → split → features → train → calibrate → threshold → evaluate → error_analysis`, nine stages — as a content-hashed DAG. **DVC tracks what the DAG consumes; MLflow owns terminal artifacts; figures are never DVC outs.** Concretely:

- **A stage's `outs:`/`metrics:`/`plots:` are small, deterministic, local-inspection artifacts** — receipts (`reports/*_receipt.json`, `cache: false`), the two cached per-row parquets (`split_manifest.parquet`, `telco_churn_features.parquet`), `reports/metrics_summary.json`, `reports/plots/decile_lift.csv`, `reports/policy/threshold.yaml`, `reports/dev_oof_diagnostics.json`. Everything with real evidentiary weight — the model itself, `training_manifest.json`, `calibration_summary.json`, SHAP values, sealed-test `metrics.json`/`economics.json`, `drift_reference.json`, `model_card.json` — is an MLflow run/`LoggedModel` artifact, resolved by `run_id`/`logged_model_id`, never by a fixed `reports/` path (see MLflow Model Registry above).
- **Figures are excluded from DVC entirely** — reliability diagrams, SHAP plots, and every other rendered chart are untracked MLflow artifacts under a run's `figures/` path, re-rendered fresh on the next genuine cycle. They carry no data DVC needs to cache or hash: nothing downstream reads a figure back in, and CLAUDE.md's "persist the evidence, not just the conclusion" rule already requires the underlying array (`dev_oof_predictions.parquet`, `shap_values.parquet`, ...) to be logged separately for exactly this reason.
- **Registration (`register.py`'s mint/tag/flip) stays outside the DAG** — a registry alias is not a file, so it cannot be expressed as a DVC stage. A full manual cycle is five steps, not one `dvc repro`: **(1)** `dvc repro calibrate` — naming `calibrate` as the target (not a bare `dvc repro`) reproduces exactly its upstream chain, `ingest → validate → split → features → train → calibrate`, and exits 0; it never attempts `threshold.py`/`evaluate.py`, which resolve their model via a *registered version* that nothing has minted yet, so there is no error to see on a normal first run — a bare `dvc repro` would instead attempt the whole graph in one call and genuinely fail there; **(2)** `register.py` (`make register-challenger`) — mints that version, tags it `pending`, points `challenger` at it; needs no explicit `register.run_id` for this default path either, since it resolves from `calibrate.py`'s own receipt the same way `dvc repro`'s stages do (an explicit `register.run_id=<id>` only matters for targeting a different historical calibration run); **(3)** `dvc repro error_analysis` — naming `error_analysis` as the target reproduces its upstream chain in turn; `calibrate` and everything before it are already cached, so what actually executes is `threshold → evaluate → error_analysis`, each resolving the version step 2 minted; **(4)** `models.review` (`make review VERDICT=... APPROVER=... NOTES=...`) — the human verdict, stamped on top of the automated gate verdict `evaluate` already computed in step 3; **(5)** `register.py register.model_version=<version>` (`make register MODEL_VERSION=<version>`) — reads the gate verdict + human review, flips `champion` or tags rejected. Steps 4-5 read as generically "review then register," but review is not spliced *between* calibrate and threshold — it runs after step 3, once `evaluate`'s verdict exists to review.
- **`params:` exemptions — four categories, none of them declared, by design:**
  1. **Env-resolved keys** (`mlflow.tracking_uri`, `database.url`) — both are `${oc.env:...}` interpolations; DVC parses `params:` files with its own YAML reader, not Hydra's, so a declared env-resolved key would hash the literal `${oc.env:...}` string forever rather than the value it actually resolves to — silently un-tracking the param while looking handled.
  2. **Identity tokens** (`run_id`, `model_version`, `logged_model_id`, `eval_run_id`, `error_analysis_run_id`, ...) — these name *which artifact from a prior cycle* a stage reads, not a modelling decision. Declaring one would make every stage rerun the instant any upstream stage produces a new run, which is what the receipt-based `deps:` edges (`reports/train_receipt.json`, `reports/calibrate_receipt.json`, ...) already express correctly.
  3. **`paths.*` — the whole prefix**, unconditionally. These keys restate `outs:`/`deps:` locations already declared by path elsewhere in the same stage, and several are `${...}`-interpolated (`${paths.reports}/figures`), which DVC cannot hash meaningfully. A repointed `paths.*` key is a correspondence question (does `dvc.yaml`'s own literal path still agree with it?), not a params-invalidation one — that is `test_paths_config_matches_dvc_yaml_literals`'s job, not `params:`'s.
  4. **`mlflow.experiment_name`** — cosmetic/organisational; renaming it changes nothing about what any stage computes.

  This is enforced structurally, not by convention — `tests/unit/test_architecture.py::test_params_match_reads` asserts every resolvable `cfg.<key>` read in a stage's real import closure is declared (`read ⊆ declared`, hard-fail from day one; the reverse direction, phantom-param detection, is advisory/future work — a clean result there is not proof nothing is over-declared). `test_loaded_configs_are_declared` covers the file-granular case for `configs/costs.yaml`/`configs/model_promotion.yaml`, which are read via explicit `OmegaConf.load(<path>)` and never enter the composed `cfg` tree the key-granular guard scans. `test_stage_deps_cover_the_real_import_closure` is the same idea applied to `deps:` — every `telco_churn.*` module a stage's entry point transitively imports must be declared or named in a standing exemption.

## Modelling Invariants

These are written policy — not notebook conventions — and must be preserved in all `src/` code:

- **Test set touched once.** Only `models/evaluate.py` may bind the test half of `data/split.py::partition()`'s `(dev_df, test_df)` return, or call `test_ids()` — enforced by `tests/unit/test_architecture.py::test_only_evaluate_binds_the_test_partition` and `::test_test_ids_is_never_called_outside_evaluate`. Downstream modules may still carry local variables literally named `y_test`/`X_test` (`error_analysis.py` does) — those are sourced from `evaluate.py`'s already-stamped `test_predictions.parquet`, a naming choice on scored output, not a second import of the sealed split. Under continuous retraining (Phase 10), the sealed test set is **not** reused for challenger-vs-champion comparisons — that erodes it; use a rolling holdout instead.
- **`evaluate.py` resolves its model by explicit `run_id` or version number, never by alias.** An alias is a moving pointer — under Phase 10's weekly retrain, `challenger` moves to a new candidate every cycle, so resolving "whatever `challenger` currently points at" can silently evaluate a different model than the one a caller intended. Log the resolved version into `reports/metrics.json` so metrics are attributable to a specific artifact. A refactor to `models:/telco-churn-pipeline@challenger` is a correctness regression, not a simplification — enforced by `tests/unit/test_architecture.py::test_evaluate_never_resolves_a_model_by_alias`.
- **One metric drives selection.** PR-AUC (average precision) is the sole criterion for model selection, Optuna tuning, and champion promotion. If two metrics point in different directions, PR-AUC wins — mixing selection signals introduces cherry-picking risk.
  - **Guardrails may veto; they may never promote.** Three metrics — recall at the shipped threshold `t*`, Brier, and the **calibration slope** — can *reject* a model that PR-AUC admitted, on the grounds that it is not a valid artifact (it misses too many churners at the operating point, it is probabilistically unskilled, or its posteriors are not honest). None can ever move the decision in the admitting direction: no model is promoted *because* its Brier improved. A veto-only guardrail cannot cherry-pick — it can only cost a promotion, never buy one — which is why this does not breach the invariant above. All remaining metrics (precision, F1, ROC-AUC, ECE, lift, expected value) are diagnostics: reported, never binding.
  - **Two calibration guardrails, because Brier alone cannot do the job.** `Brier = reliability − resolution + uncertainty` (Murphy) blends calibration with *ranking*, so a challenger that ranks better and calibrates worse **improves its Brier** and passes a Brier-only check. That failure is not cosmetic here: `t* = c/(r × LTV)` is Bayes-optimal only against honest posteriors, so a silently miscalibrated champion means the shipped threshold has stopped being the right decision rule — and no PR-AUC check would reveal it, because ranking is precisely what still looks fine. The **calibration slope** (regress `y` on `logit(p)`; 1.0 is perfect, < 1 is overconfident) is binning-free, parameter-free, and cannot be bought with better ranking, which is what makes it gate-worthy where **ECE is not** (binning-dependent, improper, biased — reported, never gating).
  - **The gate is specified in one place: `ANALYSIS.md` §0.** Its bars, its two regimes (cold start with no incumbent champion vs. comparative against a serving one), and the rationale for each live there. `models/gate.py` implements it as a pure function (`decide_promotion`), `models/evaluate.py` calls it once the sealed-test metrics exist, and `models/register.py` **reads** the persisted verdict rather than recomputing it; `PROJECT_PLAN.md` says where in the lifecycle each runs. None restates the rule — if a bar needs changing, change §0 and let the others follow.

## Project Structure (target — being built phase by phase)

```
src/telco_churn/        # importable Python package
configs/                # Hydra YAML configs (model params, paths, thresholds)
sql/                    # Postgres schema + feature-engineering SQL views
tests/unit/             # pytest unit tests (≥80% coverage target on src/)
tests/integration/      # integration tests (Postgres, API, pipeline smoke)
pipelines/              # Prefect flows (retrain, drift_check, batch_predict)
monitoring/             # Prometheus config + Grafana dashboard JSON
.github/workflows/      # CI (ci.yml), CD (cd.yml), data quality (data-quality.yml)
```

## Git Conventions

- **Branch prefix:** `feat/`, `fix/`, `chore/`, `docs/`, `test/`, `refactor/`, `data/`, `model/` — e.g. `feat/phase-0-tooling`, `fix/schema-null-handling`, `data/phase-1-ingest`.
- **Commit style:** Conventional Commits — `feat:`, `fix:`, `chore:`, `docs:`, `test:`, `refactor:`, `data:`, `model:`.
  - `data:` — dataset changes, schema updates, DVC pipeline edits, SQL changes.
  - `model:` — hyperparameter tuning, training logic, calibration, threshold changes.
- **Base branch:** `main`. Open a PR for each phase or significant feature; do not commit directly to `main`.
- **Flag any schema-breaking changes** before implementing them — raise it explicitly rather than proceeding silently.

## Changelog Conventions

Update `CHANGELOG.md` at the end of every phase or significant fix. Follow [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

**Audience:** engineers consuming the project. Recruiters are a secondary audience — the italic summary per version handles them; the bullets do not need to.

**Structure per version:**
- Version header: `## [X.Y.Z] - YYYY-MM-DD — Phase N: Short Title`
- Italic summary (1–3 sentences): what the phase accomplishes and any key architectural boundary. Keep it concise.
- `### Added / ### Changed / ### Fixed` sections as needed.

**Bullet rules:**
- 1–2 lines max per bullet. State what changed; add a short *why* clause only if non-obvious.
- Include file paths and function names so the entry is navigable (`src/telco_churn/features/build.py`).
- Keep test counts and coverage numbers — they are concrete signals worth preserving.
- **Do not embed data statistics** (churn rates, median values, percentage gaps) — those belong in `ANALYSIS.md`.
- **Do not explain implementation rationale** in depth — that belongs in commit messages or PR descriptions.
- Fixed items: one clause on what was wrong, one clause on what was done. Stop there.

## Code Style

- **Formatter:** `black` (line length 88) has final say — `.pre-commit-config.yaml` only gates `black`, not `ruff format`. `ruff format` and `black` disagree on a handful of edge cases (e.g. chained ternaries) that neither project's config can fully reconcile, so running `ruff format` alone can leave a file that fails the `black` pre-commit hook. Always run `make format` (`ruff format` then `black`, in that order) rather than `ruff format` standalone. `ruff` itself is for linting (replaces flake8/isort).
- **Type hints:** required on all public functions in `src/`. `mypy --strict` on `src/` only.
- **`__all__` required in every public module under `src/`.** List every public function, class, and constant. Omit underscore-prefixed internals. Place it after the imports block, before the first definition. **"Public" excludes two shapes:** any `__main__.py` entry point (declares no importable surface — it exists only to be run via `python -m`), and a package `__init__.py` that imports nothing from `telco_churn` (an empty placeholder — today, `serving/`, `ui/`, `monitoring/`, `utils/__init__.py`, and the root `__init__.py`). An `__init__.py` that *does* re-export project symbols, as `data/__init__.py` does, is a real API surface and is held to the rule — enforced by `tests/unit/test_architecture.py::test_every_public_module_defines_dunder_all`.
- **All functions must have docstrings.**
- **No comments explaining what the code does** — use clear names instead. Only comment the non-obvious *why* (hidden constraints, workarounds, subtle invariants).
- **No error handling inside pure functions.** Handle errors at system boundaries only: file I/O, DB calls, model load, external API calls.
- **Never use bare relative paths for file I/O in `src/`.** `"configs/config.yaml"` resolves from the shell's CWD and breaks in DVC stages, Prefect workers, CI, and Docker containers. Always anchor to `get_project_root() / "configs" / "config.yaml"` (`utils/paths.py`). This applies everywhere in `src/` — `__main__` blocks, module-level constants, and function bodies.
- **Every `src/` entry point that calls `compose_config()` must also call `activate_config(cfg)`** immediately after, so modules that resolve their own config via `load_config()` internally (rather than taking `cfg` as a parameter — e.g. `features/accessor.py`) see the same composed, override-applied tree the entry point resolved, not `config.yaml`'s on-disk defaults. Pair with `reset_active_config()` between in-process test runs (`tests/unit/conftest.py`'s autouse fixture) — enforced by `tests/unit/test_architecture.py::test_every_compose_config_call_site_also_calls_activate_config`.
- **No module under `src/` may import a name from another module that contains a `__main__` block, except a named allowance.** A `__main__` block marks a stage entry point — the module a human or `make` target runs directly, with its own argparse/`compose_config`/exit-code contract — and importing library code back out of one couples two entry points' internals together. Shared logic that needs to cross that boundary belongs in a plain (non-`__main__`-bearing) module instead — `models/calibration_metrics.py`, `models/artifacts.py`, `models/dev_features.py`, `models/policy_config.py`, `models/gate.py`, and `utils/` all exist because something calibration/threshold/evaluate/error_analysis/register needed shared without importing one another. A short allow-list captures the pre-existing legitimate exceptions (the data/features stage modules' own dev/test-partition and raw-data-validation cross-imports) — enforced by `tests/unit/test_architecture.py::test_no_module_imports_from_a_dunder_main_bearing_module`. `calibrate.py` and `register.py` no longer cross-import at all: `register.py`'s mint step (`register_challenger`) resolves everything it needs — `run_id`/`model_uri`/`logged_model_id` — from an explicit CLI override or `calibrate.py`'s own `reports/calibrate_receipt.json`, never from an in-process import, since it runs as a separate CLI invocation after `calibrate.py` has already exited.
- **All `logger.error(...)` calls inside `except` blocks must include `exc_info=True`.** `str(e)` logs only the message; `exc_info=True` attaches the full traceback so failures are diagnosable in CI and pipeline logs — enforced by `tests/unit/test_architecture.py::test_logger_error_inside_except_passes_exc_info`.
- **Notebooks** are thin wrappers: import from `src/`, call functions, render outputs. Heavy logic lives in `src/`.

## Notebook Conventions

### Heading hierarchy

Four levels, strictly nested — never skip a level:

| Level | Markdown | Usage |
|---|---|---|
| `#` | `# Title` | Notebook title — one per notebook |
| `##` | `## N. Section name` | Top-level numbered sections; unnumbered only for closing structural sections (e.g. `## Summary`, `## Provenance Output`) |
| `###` | `### Topic name` | Named topics within a section — LAPs, deep-dives, structural conclusions, analysis sub-groups |
| `####` | `#### Sub-step name` | Protocol or analysis sub-steps — `#### Construct`, `#### Evaluate`, `#### Record`, `#### Demographics` |

**No alphanumeric sub-labels on `###` in workflow/protocol notebooks** (e.g. `02a`, `02b`, future modelling notebooks). Topic headers are named descriptively; numbering adds no information and rots when sections are restructured.

**Exception — EDA and reference notebooks** (`01-eda.ipynb` and similar): `###` sections may carry alphanumeric labels (e.g. `### 6d. Numeric features vs churn`) when those labels serve as stable cross-reference addresses cited by downstream notebooks. Do not strip them.

### Cross-references between notebooks

Use `§N` notation when referring to a section in another notebook:

- Top-level section: `§6 \`01-eda.ipynb\`` → `## 6. Bivariate analysis`
- Subsection: `§6d \`01-eda.ipynb\`` → `### 6d. Numeric features vs churn`
- `ANALYSIS.md` sections: `§2` or a Markdown link `[§2 EDA](../ANALYSIS.md#2-eda--statistical-testing)`

Always use the `§N` format consistently — not "Section N", not "chapter N", not bare numbers.

## Testing

- Target ≥80% coverage on `src/`. Do not chase 100%.
- Run a single test: `uv run pytest -k "test_name"` or `uv run pytest tests/unit/test_checks.py`.
- Integration tests require a running Docker daemon, not the `docker compose --profile infra` stack — each test spins up its own ephemeral Postgres via `testcontainers` (`data.ingest`/`split`/`sql_features`/`validate` suites) and points MLflow at a tmp-scoped SQLite file (the `*_subprocess.py` suites), so `make db-up` is not a prerequisite.
- `test_train_subprocess.py` runs the full Steps 1–5 pipeline (candidates → selection → tuning → logging) against a synthetic ~150-row fixture under relaxed Hydra overrides (`tuning.n_trials=2`, etc.) — a composition-path smoke test, not a benchmark; it asserts the pipeline completes and logs the expected artifacts, not a ROC-AUC/PR-AUC bar.
- When writing data, schema, or validation tests, cover: normal case, missing values, wrong dtypes, and empty dataframe.
- Every module with a `__main__` CLI entry point requires a **subprocess** integration test — invoked via `subprocess.run([sys.executable, "-m", "<module>"], env={...}, capture_output=True)` — covering the full composition path (argparse/config load → I/O → exit code). Direct function calls do not qualify: they miss argparse, OmegaConf resolution, dotenv loading, and env-var-to-engine joints that only surface at the subprocess boundary. Cover both the exit 0 (success) and exit 1 (error) paths. Exception: waived only when the module's entire `__main__` body is exercised as a named subroutine call inside another module's subprocess integration test — direct calls to shared helper functions do not satisfy the exception. Enforced by `tests/unit/test_architecture.py::test_every_main_module_has_a_subprocess_test` (parametrized per `__main__`-bearing module; a named waiver dict holds the exception case, empty today).
- Running integration tests in isolation will fail the `fail_under=80` gate because coverage is measured across all `src/` modules. Append `--no-cov` for standalone integration runs: `uv run pytest tests/integration/ --run-integration --no-cov`.
- Every new package added under `src/telco_churn/` must have a scoped `make test-<package>` Makefile target using `--override-ini="addopts=" --cov=src/telco_churn/<package>`. The global `make test` remains the CI gate; the scoped target lets phase tests run in isolation without a false `fail_under=80` failure from other packages.
- Run `pytest` before marking any task complete; if the phase has no tests yet, note it explicitly instead of skipping.
- When closing a QA backlog item (`[x]`), verify the fix is present in the code — read the relevant file or run the relevant test. Never mark `[x]` based on intent; only mark it after the change is confirmed in the working tree.

## Environment Variables

Copy `.env.example` to `.env` for local development. Never commit `.env`. Required vars (set up per phase):

```
POSTGRES_URL=postgresql://user:pass@localhost:5432/telco_churn  # pragma: allowlist secret
MLFLOW_TRACKING_URI=http://localhost:5000
PREFECT_API_URL=http://localhost:4200/api
AWS_REGION=us-east-1
```

## Phase Checklist (quick reference)

Lifecycle-ordered. Tests are written alongside each module — there is no dedicated "tests phase."

| Phase | Goal | Key deliverable |
|---|---|---|
| 0 | Project foundation | `pyproject.toml`, `uv.lock`, `.pre-commit-config.yaml`, skeleton dirs, Hydra root, structlog |
| 1 | Data ingestion (CSV → Postgres) | `docker-compose.yml` w/ Postgres + `sql/schema/001_create_raw.sql` + `data/ingest.py` |
| 2 | Data validation (Pandera + 5 gates) | `data/schema.py` + `data/checks.py` + `data/validate.py` + `tests/unit/test_checks.py` + `tests/unit/test_validate.py` |
| 3 | EDA notebook (slim rewrite) | `notebooks/01-eda.ipynb` importing from `src/`; original archived |
| 4 | Feature engineering (SQL + Python) | `sql/features/*.sql` + `features/sql_features.py` + `features/build.py` |
| 5 | Model training (LightGBM + Optuna + MLflow) | `models/train/` + `configs/{model,training}/*.yaml`; logged only, no registration |
| 6 | Calibration + cost-sensitive threshold | `models/calibrate.py` + `models/threshold.py` |
| 7 | Evaluation + error analysis + registry promotion | `models/evaluate.py` + `models/gate.py` + `models/economics.py` + `models/explain.py` + `models/error_analysis.py` + `models/register.py` + `models/drift_reference.py` + `notebooks/05-evaluation-and-error-analysis.ipynb` |
| 8 | DVC pipeline wrap | `dvc.yaml` with 9 stages; reproducible end-to-end |
| 9 | Serving + UI | FastAPI (`/predict`, `/health`, `/metrics`) + Streamlit + Dockerfiles |
| 10 | Orchestration | Prefect retrain flow (weekly) + drift check flow (daily) |
| 11 | CI/CD | GitHub Actions ci.yml + cd.yml + data-quality.yml |
| 12 | AWS deployment | ECR + App Runner + RDS + S3 |
| 13 | Monitoring | Prometheus + Grafana dashboards + Evidently drift |
| 14 | Docs polish | README update + runbook + architecture diagram |
