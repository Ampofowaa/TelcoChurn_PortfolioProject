# Phase 8 Prerequisites — Task List

Working checklist for `### Phase 8 Prerequisites — Config Literal, Architectural Guards + Selection Retirement` in `PROJECT_PLAN.md`. Branch: `chore/phase-8-prereqs`. Update checkboxes as work lands; this file tracks execution state, `PROJECT_PLAN.md` remains the source of truth for rationale.

**Status as of 2026-08-08:** §0 pre-work closed out. **PR A complete.** Everything through 2026-08-07 as previously recorded (`activate_config`/`reset_active_config`, the feature-selection and model-family retirements, the `common.py::_load_processed` deletion, the CSV→Parquet accessor rename), plus the remaining PR A items closed out 2026-08-08: the `data_content_hash` fix (`_dvc_hash` deleted outright, replaced by `features_sha256()` everywhere), the legible `tuning_{data_hash[:8]}_{config_digest[:8]}` study name plus its regression test, the `log_model.py` disjoint-hyperparameter-collision guard plus its test, and PR A's own `CLAUDE.md` doc corrections (the `activate_config` pairing rule, the `processed_data` reader-guard pointer, and the `training_manifest.json` `data_content_hash` field description). `configs/register/default.yaml`'s `require_review` comment is still config/doc-only, ahead of B1's code. `tests/unit/test_architecture.py` now has all guards PR A adds (18 tests); B0/B1/B2/C's own guards remain for their respective PRs. Verified: `make test-features` (143 tests, 93.84% cov), `make test-models` (492 tests, 97.06% cov), the affected-suite slice, and `tests/unit/test_architecture.py` all pass; `uv run python -m telco_churn.models.train` re-run twice more this session with no `feature_selection` CV and correct `data_content_hash` tags; `notebooks/03c-hyperparameter-tuning.ipynb` fixed for three latent staleness bugs surfaced by this work (stale `dvc_data_hash` tag references, the pre-Parquet CSV path, retired `training_setup.cv_folds`/`cv_repeats`) and re-executed clean.

**PR B0 complete (2026-08-08)** — see its own section below for the full breakdown. `main` is green for B1 to build on.

**PR B1 complete (2026-08-09)** — see its own section below for the full breakdown, including the new `models/review.py` CLI PROJECT_PLAN.md's registration/review resolution already called for but this branch had not yet built. Branch is green for B2 to build on.

**PR B2 complete (2026-08-09)** — see its own section below for the full breakdown, including the real dev-SHAP derivation run and the two config constants it produced. Branch is green for PR C to build on.

## Sequencing (hard constraints, not preferences)

1. **PR A first**, regardless of the rest — own internal ordering note.
2. **§0 pre-work before deleting any selection code** — the CV wiring cannot regenerate its own evidence once removed.
3. **PR B0 before PR B1** — hard precondition. B1 removes the registry-version resolution path; B0 is what keeps `main` green while B1 is under review.
4. **PR B1 before PR B2** — B1 unblocks stage 8's `outs:`, and B2 deletes `direction_sanity_check_fired`, which is B1's to remove.
5. **PR C last** — pure move-and-rewrite; should rebase over B1's and B2's content changes, not the reverse.

---

## §0 — Pre-work: resolve before deleting feature-selection code

Evidence cannot be regenerated after deletion — do these first or the retirement's own justification becomes unverifiable.

**§0 closed out 2026-08-06** — all six items resolved. Four fixed directly (numeric reconciliation in `ANALYSIS.md`); two relocated into PR A's feature-selection retirement block below, where they're sequenced against the actual deletion rather than gating PR A's start.

- [x] Reconcile `ANALYSIS.md` §4b's per-fold stability table against run `de4fac8e`'s `per_fold_stability.csv` artifact. **Root cause found via `git blame`:** the table was written in commit `67224f8b` (2026-07-11); `select.py`'s per-fold child-seed fix (each fold now gets its own spawned seed instead of reusing `random_state`) landed 8 days later in commit `629c121` (2026-07-19). The table predates the fix; `de4fac8e` postdates it and is the only feature-selection run with artifacts on disk — treated as current ground truth. All 15 mismatched values corrected in the table (and prose mentions) to match the artifact: `charge_per_service` 46→69, `totalcharges` 91→96, `multiplelines` 72→60, `paymentmethod` 33→38, etc.
- [x] Fix §4b's internal `paymentmethod` inconsistency — prose's "49/100" was a third, independently wrong number (written 3 days after the table in commit `6b7c254`, never matched the table's own 33/100 even at the time). Corrected to 38/100 (matches artifact) in both the intro prose and the fluke-check paragraph.
- [x] Confirm the divergence is confined to the fold-survivor path — confirmed two ways: (1) `real_importance`/`decoy_importance`/`survived` in `permutation_importance_table.csv` match the doc's table exactly (deterministic on the all-dev fit's fixed `random_state`, unaffected by the per-fold seed fix); (2) `bootstrap_delta_dist.png` shows Δ_obs = 0.008, matching `ANALYSIS.md`'s cited Δ = 0.0080 exactly. Only per-feature fold-survival counts moved.
- [x] Settle `charge_per_service` specifically — corrected 46→69/100. This also changed the fluke-check paragraph's headline claim from "8 of the 10 survivors clear 70/100" to **7 of 10** (`multiplelines` now falls to 60/100 and joins `paymentmethod`/`streamingtv` as a sub-70 survivor) — narrative rewritten accordingly, not just the numbers.
- [x] Decide `notebooks/03b-feature-selection.ipynb`'s fate — **decided 2026-08-06: rewrite as the importance-diagnostic walkthrough** (not archive). The plan's actual constraint is deciding before deletion lands, not executing the rewrite first — and the rewrite will read better once it can show the real post-retirement diagnostic surface (permutation table, SHAP audit, demographic joint importance) rather than a snapshot taken before that surface exists. Execution relocated to PR A, sequenced after the retirement lands (see below).
- [ ] ~~Verify the `decision == "reduced"` branch has never been taken across *every* logged `feature_selection` run~~ — relocated to PR A (see below). This reads MLflow run history, not live code, so it isn't actually blocked by the CV wiring's deletion; grouping it here was for due-diligence sequencing only.

---

## PR A — Data paths, config, training

- [x] `configs/config.yaml`: `paths.processed_data` as a plain literal *(already correct on branch — comment matches the plan's wording)*
- [x] `utils/paths.py`: add `activate_config(cfg)` / `reset_active_config()`
- [x] `load_config()` returns the installed/merged config when one is active, real config otherwise
- [x] Tests: `tests/unit/test_paths.py` (installed-config precedence, reset reverts to disk, no-op baseline) + autouse `_reset_active_config` fixture in `tests/unit/conftest.py`. Full unit suite (868 tests) passes; `ruff check`/`ruff format --check`/`mypy --strict` clean on all touched files.
- [x] `features/build.py:71` — `load_config()` → `compose_config(overrides=sys.argv[1:] or None)` + `activate_config(cfg)`
- [x] `data/split.py` — `compose_config()` gains `overrides=sys.argv[1:] or None` + `activate_config(cfg)`
- [x] `data/validate.py` — `load_config()` → `compose_config(overrides=sys.argv[1:] or None)` + `activate_config(cfg)`
- [x] `features/sql_features.py` — same swap (not separately called out in the plan's bullet list but same pattern, same file set)
- [x] Each remaining `__main__` entry point gets its two-line `activate_config` call — `models/train/__main__.py`, `models/calibrate.py`, `models/threshold.py`, `models/evaluate.py`, `models/error_analysis.py`, `models/register.py` all updated (11 entry points total, all wired)
- [x] `conftest.py` autouse fixture resetting the installed config between in-process tests *(landed earlier alongside `activate_config` itself)*
- [x] Architectural test: every module calling `compose_config()` also calls `activate_config()` — `test_every_compose_config_call_site_also_calls_activate_config` in `test_architecture.py` (18 tests now, all passing)
- [x] Migrate all 9 test files off `PROCESSED_DATA_DIR`:
  - [x] 11 subprocess `env={…}` sites → `paths.processed_data=<tmp>` argv override, all 8 files (`test_calibrate_subprocess.py`, `test_error_analysis_subprocess.py`, `test_register_subprocess.py`, `test_train_subprocess.py`, `test_evaluate_subprocess.py`, `test_sql_features_postgres.py`, `test_split_postgres.py`, `test_threshold_subprocess.py`)
  - [x] 6 in-process `mp.setenv` sites → `activate_config(cfg)` + `try/finally: reset_active_config()`, all 5 files (`test_error_analysis_subprocess.py`, `test_register_subprocess.py` ×2, `test_evaluate_subprocess.py`, `test_threshold_subprocess.py`, `test_error_analysis.py`)
  - [x] **Fixture-ordering hazard found and fixed in `test_error_analysis.py`**: its `evaluated_model` fixture is module-scoped and must keep the config installed across every test body in the module (unlike the other 4 files, where `activate_config`/`reset_active_config` are fully scoped inside the fixture's own setup, before it returns). Verified empirically that pytest instantiates module-scoped fixtures before function-scoped ones, so `tests/unit/conftest.py`'s autouse `_reset_active_config` would silently wipe the module fixture's installed config before every test body ran. Fixed with a second, file-local autouse fixture (`_reactivate_evaluated_model_config`, guarded on `"evaluated_model" in request.fixturenames`) that reinstalls it after the conftest reset — same-scope conftest fixtures instantiate before same-scope fixtures declared in the test module itself, so this one runs last. All 46 tests in the file pass.
  - [x] New `tests/integration/conftest.py` — autouse `_reset_active_config` backstop, mirroring the unit one (not load-bearing for the integration fixtures themselves, which already self-contain their reset, but consistent defense-in-depth)
  - [x] `ruff check`/`ruff format --check` clean on all 9 files; full integration run confirmed — all 8 migrated files, 40 tests passed in 19:08 (`b0qh1x4ug`), no behavioral regressions
- [x] Delete `ingest.py`'s `argparse` block — removed together with its `activate_config` wiring (coupled: `compose_config(overrides=sys.argv[1:])` and `argparse.parse_args()` can't both read `sys.argv`); its two subprocess callers (`test_ingest_postgres.py`) moved to `paths.raw_data=<path>`; `CHANGELOG.md` `### Changed` entries added for the config-literal, `activate_config`, and `--csv-path` removal
- [x] **Feature-selection retirement — do in this order:** *(all 11 sub-items below landed 2026-08-06/07; parent checkbox lagged the work — checked off 2026-08-07)*
  1. [x] Verify the `decision == "reduced"` branch has never been taken across *every* logged `feature_selection` run, not just `de4fac8e` *(relocated from §0)* — the retirement's justification rests on this; confirm before deleting the code that could otherwise re-verify it. **Confirmed 2026-08-06**: queried the live Postgres-backed MLflow server (experiment `telco-churn-training`, id 1) with `run_view_type=ALL` (includes deleted) across both experiments — exactly one `feature_selection`-named run exists total, `de4fac8ec5a144fc86eb30264679b994`, `decision=full`. No other run, live or deleted, carries that run name or a `decision` param. The local SQLite fallback (`mlflow.db`, gitignored, unused by this project's actual training runs) is empty.
  2. [x] Retire the feature-selection CV wrapper *(confirmed fully intact today: `run_selection_cv`, `reduced_set_bootstrap_test`, `_run_selection_cv_and_bootstrap`, `feature_audit.py`'s `decision == "reduced"` branch, `_plot_selection_figures`, and their tests)*. **Done 2026-08-06**: `select.py` loses `run_selection_cv`, `reduced_set_bootstrap_test`, `_fit_and_score_selection_fold`, `_build_selection_pipeline`, and their `__all__` entries; kept `mint_committed_list`, `decide_survivors`, `compute_shap_audit`, `flag_high_shap_dropouts`, `PermutationImportanceSelector` (now also exposes `group_importance_`). `feature_audit.py` rewritten: `run_selection_step(X_dev, y_dev, cfg)` (dropped the now-unused `candidate_results` param) always commits the full `FEATURE_SCHEMA` space, runs one all-dev `mint_committed_list` fit + `compute_shap_audit`, and logs `selection/group_importance.json` alongside the existing artifacts — no more CV, bootstrap test, decision branch, or full-vs-reduced PR/bootstrap-delta figures. `__main__.py`'s call site updated to match.
  2b. [x] **Follow-up (2026-08-07):** renamed `run_selection_step` → `run_feature_audit_step` (`feature_audit.py`) — the old name implied an action ("select") the function no longer performs since the CV-based decision was retired in the item above; it only reads the already-frozen `COMMITTED_FEATURES` and logs a diagnostic audit against it. Propagated through `models/train/__init__.py`, `models/__init__.py`, `models/train/__main__.py` (which also gained a comment naming all five pipeline steps above its Step 3-5 calls), `models/calibrate.py`'s docstring, `test_train_feature_audit.py`, `test_train_subprocess.py`, and `notebooks/03b-feature-selection.ipynb`. Module name (`feature_audit.py`) left unchanged — already matches the codebase's "frozen"/`COMMITTED_FEATURES` vocabulary and is cited by exact line number throughout PROJECT_PLAN.md's Phase 8 planning prose, too large a sync burden for the actual point of confusion (the function name, not the module name).
  2c. [x] **Follow-up (2026-08-07):** the "On-demand ablation review" section §3 of `03b-feature-selection.ipynb` rewrote — hand-inlined orchestration code (and, separately, stale: it read the retired `training_setup.cv_folds`/`cv_repeats` keys and would have raised `ConfigAttributeError` if run) — into a new packaged module, `models/train/feature_selection.py::run_feature_selection_step`, the same on-demand-trigger role `candidates.py`/`comparison.py` play for the model-family decision (module briefly named `selection_review.py`/`run_selection_review_step` before a same-day rename — the original name undersold that it *decides* `COMMITTED_FEATURES`, not merely reviews it). Also fixed along the way: `select.py::run_selection_cv` now aggregates per-fold `permutation_importance_table_`/`group_importance_` (mean across all 100 folds, from fits it already performs) instead of discarding them, so the review's own recommended-feature list, logged table, and SHAP audit are derived from that 100-fold-averaged signal (`stability >= 0.5`) rather than a second, separately-fit `mint_committed_list` call whose single draw could disagree with the fold-survival picture (the "minting defect" this prereq's item 2 above already documents). New `common.py::_plot_shap_audit` (mirroring `_plot_bootstrap_delta`'s split of computation vs. plotting) renders and logs a SHAP chart — green/red/gold for committed/not-committed/high-SHAP-dropout — for both `feature_audit.py` and `feature_selection.py`; §3 of the notebook now downloads and displays its own review's chart instead of leaving the SHAP evidence as text only. `feature_audit.py`/`feature_selection.py` MLflow artifacts also moved off their blanket `selection/`/`review/` prefix onto the run root (figures under `figures/`), matching `CLAUDE.md`'s already-stated rule for `calibrate.py`/`threshold.py`/`evaluate.py` that a redundant stage-name prefix is unneeded when the run itself already is that stage. `tests/unit/test_train_feature_selection.py` (new, 11 tests), `test_select.py` (+2), `test_train_feature_audit.py` updated for the new artifact layout. Full `train`/`select` unit slice re-run clean throughout; `ruff`/`mypy --strict` clean on every touched file.
  2d. **Follow-up (2026-08-08):** confirmed via the Phase 8 crosscheck full-pipeline rerun that item 2's "Done" description above and item 6 below are both stale — `run_selection_cv`, `reduced_set_bootstrap_test`, `_fit_and_score_selection_fold`, and `_build_selection_pipeline` were never actually deleted from `features/select.py`; all four are still defined there today, and `run_selection_cv`/`reduced_set_bootstrap_test` remain in its `__all__`. What item 2c above actually did (same day, written after item 2's text) was retarget their caller, not delete them: `feature_audit.py` stopped calling them (it now only cites `run_selection_cv` in a docstring), and the new `models/train/feature_selection.py::run_feature_selection_step` became their sole caller, powering `03b`'s §2 on-demand review. Item 2/6's "deleted"/"pruned" language describes the intent as it stood before 2c's design changed, not the current state of `select.py` — read both as superseded by 2c.
  3. [x] Add demographic `correlated_groups` entry `[gender, seniorcitizen, has_partner, dependents]` to `configs/training/feature_selection.yaml` *(confirmed only `[tenure, totalcharges, monthlycharges]` present today)*. **Done 2026-08-06.**
  4. [x] Confirm the demographic-block rescue actually fires once declared; record the returned `group_importance`. **Confirmed 2026-08-06** against real dev data: all four members fail the individual floor (gender −0.00088, seniorcitizen 0.00300, has_partner −0.00104, dependents −0.00113, vs. floor 0.005) — the joint-permutation trigger condition is satisfied and fires, returning **group_importance = 0.00116**, still below the 0.005 floor, so `survived`/`rescued` stay `False` for the block (matches the plan's framing: the trigger fires and prices the block, survival is a separate question). 10/20 features committed on this run, matching §4b's prior full-feature-space diagnostic.
  5. [x] Delete or re-scope `training_setup.cv_n_jobs` — dead once `run_selection_cv` goes, unless repurposed by the optional `logreg_cv` item below. **Deleted 2026-08-06** from `configs/config.yaml` (and the stale referencing comment in `candidates.py`); not repurposed since the optional `logreg_cv` n_jobs item is unstarted.
  6. [x] Prune `features/select.py`'s `__all__` of every deleted symbol; check no other importers. **Done 2026-08-06** — only `feature_audit.py` and the two test files imported the retired symbols; both updated.
  7. [x] `configs/training/feature_selection.yaml` loses `bootstrap_n_samples`. **Done 2026-08-06.**
  8. [x] `test_train_feature_audit.py` / `test_select.py` shed the deleted-function test cases; `test_train_subprocess.py`'s Step 3 assertions updated. **Done 2026-08-06**: `test_select.py` drops the `run_selection_cv`/`reduced_set_bootstrap_test` sections (21 tests remain, all passing) and gains two new tests for `PermutationImportanceSelector.group_importance_`. `test_train_feature_audit.py` rewritten for the new `run_selection_step(X_dev, y_dev, cfg)` signature and return contract (7 tests, all passing). `test_train_subprocess.py`'s fast-override list drops `selection.bootstrap_n_samples=200` (key no longer exists). Verified: full `train`/`select`/`architecture` unit slice (144 tests) and the `test_train_subprocess.py` integration suite (2 tests, full Steps 1-5 pipeline) both pass.
  9. [x] `CHANGELOG.md` `### Changed` entry for the selection-step retirement. **Done 2026-08-06** — `### Changed`/`### Removed` entries added under `[Unreleased]` covering the retired functions, the new `group_importance_` exposure, and the demographic `correlated_groups` addition.
  10. [x] `ANALYSIS.md` §4b revised (not rewritten) — reframe as a concluded ablation, cite the run id, add the demographic joint-importance result. **Done 2026-08-06**: header/intro reframed as "concluded ablation + importance diagnostic", new **Retirement** paragraph citing `de4fac8e` and the ~85,000-scoring-pass cost, and a new point 4 under "Protected attributes & fairness policy" recording the demographic block's individual failures and `group_importance = 0.00116`. §4c's cross-reference updated to note the input space is now a fixed constant.
  11. [x] Rewrite `notebooks/03b-feature-selection.ipynb` as the importance-diagnostic walkthrough *(relocated from §0 — decided 2026-08-06: rewrite, not archive)*, last, so it reflects the retirement's final shape rather than a pre-retirement snapshot. **Done 2026-08-06**: dropped the per-fold-stability section (no more CV artifact) and the `lgbm_default`/`feature_selection` cross-validation cell (no more shared `candidate_results`); the internetservice-addon illustration replaced with the demographic-block rescue check (now sourced from `cfg.selection.correlated_groups`, not the code-level `DEFAULT_CORRELATED_GROUPS` constant, to match what's actually configured). Ran a full real `python -m telco_churn.models.train` cycle to produce a current `feature_selection` run (`e620cc7c...`) carrying `group_importance.json`, then executed the notebook end-to-end via `jupyter nbconvert --execute` — 0 errors, every rendered number (decoy floor, survivor counts, `group_importance = 0.00116`) matches `ANALYSIS.md` §4b.
    - **Follow-up (same day):** the first pass's §3 still *re-rendered* the retired ablation from MLflow (fold-history pulls, PR-curve/bootstrap-delta PNGs) via a code cell — dead-code-shaped surface referencing mechanisms no longer in `src/`, which is exactly what the retirement's own rule (`ANALYSIS.md`/`CHANGELOG.md`: the record needs no live callsite) argues against. Replaced with `## 3. Why every feature ships, including the ones that fail the floor` — one markdown-only citation paragraph (the numbers, `de4fac8e`, a link to §4b) followed directly by the actual current-state analysis ("Reasons for keeping the failed features," folded in as the section's main content rather than a subsection after a historical wall of code/plots). Notebook shrank 30→27 cells, code cells 9→8; re-executed clean, 0 errors.
    - **Follow-up cleanup:** `DEFAULT_CORRELATED_GROUPS` (`select.py`) turned out to be dead — nothing invoked its default value (production always threads `cfg.selection.correlated_groups`; every test and the rewritten notebook pass `correlated_groups` explicitly). Removed the constant and its `__all__` entry; `correlated_groups` is now a required (no-default) parameter on `PermutationImportanceSelector.__init__` and `mint_committed_list`. 5 call sites in `test_select.py` updated to pass `correlated_groups=()` explicitly where the test doesn't exercise rescue behaviour.
- [x] **Model-family retirement — mirrors the feature-selection retirement above, same reasoning applied to the adjacent axis.** `__main__.py` re-ran the full Dummy/LogReg/LightGBM comparison and paired-bootstrap family decision (Steps 1-2) on every training cycle — including every one of Phase 10's future weekly retrains — for a decision `ANALYSIS.md` §4a already records as concluded. **Done 2026-08-07:**
  1. [x] New `models/train/common.py::COMMITTED_MODEL_FAMILY` (`"lightgbm"`) + `COMMITTED_MODEL_FAMILY_DECISION_RUN_ID`, mirroring `features/schema.py::COMMITTED_FEATURES`'s discipline. Resolved the decision run id by querying the live Postgres-backed MLflow server (experiment `telco-churn-training`, `model_comparison`-named runs) rather than guessing: two candidate runs existed with identical numbers (Δ=0.007, CI [0.002,0.012]); took the more recent one whose `git_sha` tag matches current `HEAD` (`01a0100b92a34c7a964879cf541fd6f3`, 2026-08-06).
  2. [x] `__main__.py` no longer calls `run_candidate_step`/`run_comparison_step`; the LogReg-winner warn-and-skip branch (the item below this one, previously tracked as a TODO to add `sys.exit(1)`) is deleted outright rather than fixed — there is nothing left to skip against once the family is a frozen constant. That tracked TODO is superseded, not done as originally written.
  3. [x] `candidates.py`/`comparison.py` module docstrings updated: notebook-only (`03a-model-selection.ipynb`), not called from the automated pipeline. `run_candidate_step`/`run_comparison_step`/`run_diagnostics_step` gain explicit `cv_folds`/`cv_repeats`/`n_bootstrap`/`segment_n_bootstrap` parameters defaulting to new `_FAMILY_REVIEW_*` module constants — kept as real function parameters (not inlined) specifically so existing unit-test fixtures can still override them for speed, the same role `reduced_set_bootstrap_test`'s `n_bootstrap` parameter already plays for the feature-selection ablation.
  4. [x] `log_model.py`: `_build_training_manifest`'s `model_comparison` section (recomputed paired-Δ) replaced by `model_family_committed` (`model_family`, `decision_reference: "ANALYSIS.md §4a"`, `decision_run_id`); `run_model_logging_step` drops the now-unused `comparison` parameter.
  5. [x] `configs/config.yaml`: `training_setup.cv_folds`/`cv_repeats`/`bootstrap_n_samples`/`segment_bootstrap_n_samples` removed — confirmed via grep these were consumed only by Steps 1-2, nothing else in `src/`. `delta_threshold`/`fixed_recall_thresholds` kept (load-bearing in `calibrate.py`/`evaluate.py`). Stale cross-referencing comments in `configs/tuning/optuna.yaml`, `configs/evaluate/default.yaml`, `configs/calibration/default.yaml` (which named the old `training_setup.*` keys for contrast) updated to point at the new module constants instead.
  6. [x] `notebooks/03a-model-selection.ipynb` rewritten from passive renderer ("already ran via `python -m telco_churn.models.train`") to on-demand trigger — §1 now calls `run_candidate_step`/`run_comparison_step` directly, logs fresh MLflow runs, and prints the `model_comparison` run id to cite. §4's bias/variance loop reuses the same loaded dev partition instead of a second independent load, and its own CV construction switched off the retired `cfg.training_setup.cv_folds/cv_repeats` onto the `_FAMILY_REVIEW_*` constants. Executed end-to-end via `jupyter nbconvert --execute` to confirm it runs clean.
  7. [x] Test fixtures updated: `test_train_candidates.py`/`test_train_comparison.py` call sites now pass `cv_folds`/`cv_repeats`/`n_bootstrap`/`segment_n_bootstrap` explicitly (sourced from their existing cfg fixtures) instead of relying on the functions reading them from `cfg.training_setup` — required once the config keys were retired, or the fixtures' small test values would've been silently ignored in favour of full-scale production constants. `test_train_log_model.py`'s `comparison_result` fixture and parameter removed; manifest assertions updated for `model_family_committed`. `test_train_common.py::test_compose_config_loads_expected_structure` drops its assertions on the two removed keys. `test_train_subprocess.py`'s `_FAST_OVERRIDES` drops the six now-dead Step 1-2 override lines; docstring updated to describe the Steps 3-5 composition path.
  8. [x] `ANALYSIS.md` §4a gains a "Retirement" paragraph (mirrors §4b's), `PROJECT_PLAN.md` Phase 5 gains a ⚠ note beside the existing Phase 8 one, Phase 10 gains a resolved 📌 bullet beside the existing feature-selection one, `CLAUDE.md`'s `training_manifest.json` spec line corrected, `CHANGELOG.md` `### Changed`/`### Removed` entries added.
- [x] `data_content_hash` fix (`tuning.py:309-320`): rename `"dvc_hash"` key → `"data_content_hash"`; land **after** `activate_config` (§ PR A above) and **in the same PR as, or before,** the selection retirement — not after it. **Done 2026-08-08**: went beyond the literal rename — `_dvc_hash` was dead code (read a `.dvc` sidecar that structurally can never exist for a pipeline-stage output) and is deleted outright from `common.py`, along with its now-unused `yaml`/`get_project_root` imports. All six former callers (`candidates.py`, `comparison.py`, `feature_audit.py`, `feature_selection.py`, `log_model.py`, `tuning.py` ×2) now tag/log `"data_content_hash"` sourced from `features/accessor.py::features_sha256()` — a real, unconditional sha256 of the processed-features parquet, independent of DVC. `test_train_common.py` drops its three `_dvc_hash`-specific tests and the section comment; `test_train_candidates.py`'s two `monkeypatch.setattr(candidates, "_dvc_hash", ...)` sites are dropped entirely (real hash over the test's own on-disk fixture parquet is cheap, and patching to `"unknown"` would just reintroduce the bug this fix removes). `test_architecture.py`'s allow-listed `models/train/common.py` entry (from item 85 below) removed now that `common.py` no longer reads `cfg.paths.processed_data` at all.
- [x] Legible study name: `f"tuning_{data_hash[:8]}_{config_digest[:8]}"`. **Done 2026-08-08**: `_study_name` now hashes `data_hash` (`features_sha256()`) and `config_digest` (the rest of the former key — search space, CV/early-stopping/pruner settings, `committed_features`) as two independent sha256 digests, truncated to 8 hex chars each, so a human scanning the `optuna` schema or MLflow UI can tell at a glance whether a new study came from a data change or a config change without decoding anything.
- [x] Test: hold `cfg` + `committed_features` fixed, point at two different-content features files, assert two different study names. **Done 2026-08-08**: `test_study_name_changes_when_features_content_changes` (`test_train_tuning.py`) — two tmp dirs each holding a differently-content `telco_churn_features.parquet`, switched via `activate_config`/`reset_active_config`; asserts the two resulting study names differ, and that re-activating the first dir reproduces the first name exactly (unchanged data resumes the same study).
- [x] ~~`models/train/__main__.py:39-53` — LogReg branch: add `sys.exit(1)` in the `else`~~ — **superseded 2026-08-07 by the model-family retirement above**, not fixed as originally written. The whole `if comparison["decision"] == "lgbm": ... else: ...` branch is deleted, not patched: `run_candidate_step`/`run_comparison_step` no longer run in `__main__.py` at all, so there is no live `comparison["decision"]` to branch on. See the model-family retirement item above.
- [x] `log_model.py:389` — assert `selected_hyperparameters`/`fixed_hyperparameters` are disjoint, raise naming the colliding key *(confirmed no guard exists today)*; document the derived-params exclusion in the `fixed:` block's comment. **Done 2026-08-08**: `run_model_logging_step` now raises `ValueError` naming the exact colliding key(s) (`sorted()`, so the message is deterministic) before the merge; `configs/training/lightgbm.yaml`'s `fixed:` block gains a header comment stating the disjointness the guard enforces, so a future key added there that collides with `configs/tuning/optuna.yaml`'s search space fails loudly instead of silently deciding a value Optuna also searched. `test_run_model_logging_step_raises_on_hyperparameter_collision` (`test_train_log_model.py`) monkeypatches `_lgbm_fixed_knobs` to force a collision and asserts the raise names the key.
- [x] 🔴 `common.py` — delete `_load_processed`, route `_load_dev_features` through `features/accessor.py::load_features()` — must land **before** the CSV→Parquet rename, not with it. **Done 2026-08-07**: `_load_processed` removed; `_load_dev_features` now calls `load_features()` directly and drops its now-unused `cfg` parameter (call site in `train/__main__.py` updated). `test_train_common.py`'s `_load_processed` schema-guard tests removed (redundant with `test_accessor.py`'s `load_features()` coverage); its `_load_dev_features` tests retargeted to monkeypatch `common.load_features`.
  - **CSV→Parquet rename landed the same day, immediately after this prereq** (PROJECT_PLAN.md's own §8 note on this item flags the ordering as a correctness constraint, not a preference — the two must not land in the other order). `features/accessor.py::FEATURES_FILENAME` → `telco_churn_features.parquet`, `load_features()`'s `pd.read_csv` → `pd.read_parquet`; `features/build.py`'s `__main__` writes Parquet via the same constant. All 7 test files seeding a synthetic processed-features fixture (`test_calibrate_subprocess.py`, `test_error_analysis_subprocess.py`, `test_register_subprocess.py`, `test_evaluate_subprocess.py`, `test_threshold_subprocess.py`, `test_train_subprocess.py`, `test_error_analysis.py`) and `test_accessor.py`/`test_sql_features_postgres.py` updated to match; the two CSV-specific edge-case tests (`test_load_features_empty_file_raises`, `test_load_features_header_only_raises`) rewritten for Parquet's actual failure modes (verified empirically: a zero-byte file raises `pyarrow.lib.ArrowInvalid`, not `pd.errors.EmptyDataError`; a zero-row file round-trips its dtypes and *passes* validation, unlike CSV's all-`object`-dtype header-only read — confirmed with a real `FeatureOutputSchema.validate()` call before writing the test). Stale local `datasets/processed/telco_churn_processed.csv` deleted (gitignored, not tracked) per PROJECT_PLAN.md's explicit instruction; **`make features` needs a re-run before `make train` works locally again.** `_dvc_hash`'s own hardcoded `telco_churn_processed.csv.dvc` reference deliberately left untouched — it's retired by the separate `data_content_hash` item above, not this one. Full unit suite (868 tests) and the two live-Postgres `test_sql_features_postgres.py` integration tests re-run clean; `ruff`/`mypy --strict` clean on all touched files.
- [x] `test_architecture.py` guard: `cfg.paths.processed_data` readable only in `features/accessor.py` and `data/split.py` *(confirmed not among the current 16 tests)* — add after the deletion above, not before. **Done 2026-08-07**: `test_processed_data_path_is_read_only_by_its_canonical_resolvers` added. Scope widened by one module vs. the plan's original two: `features/build.py`'s `__main__` was reading `cfg.paths.processed_data` directly to build its output path — refactored to resolve the write path via `features/accessor.py::features_path()` instead, closing that site. `models/train/common.py::_dvc_hash` still reads it directly (allow-listed with an inline comment) — that read is tied to the excluded `data_content_hash` fix above, not this item; remove the allowance when that fix lands. 18 tests pass in `test_architecture.py`; full unit suite, `ruff`, `mypy --strict` clean on all touched files.
  - **Follow-up (2026-08-08):** the `data_content_hash` fix landed (item 78 above) and deleted `_dvc_hash` outright, so `common.py` no longer reads `cfg.paths.processed_data` at all — the allowance is removed from `_PROCESSED_DATA_PATH_READERS`, as promised. `features/accessor.py` and `data/split.py` are now the only two readers, matching the plan's original scope exactly.
- [x] Optional/measured: `n_jobs=-1` for `logreg_cv` folds only, via `training_setup.cv_n_jobs`; verify with an exact 100-value fold-metric diff; keep only if it pays. **Tried 2026-08-07, reverted 2026-08-08**: implemented as a module constant `_LOGREG_CV_N_JOBS = -1` in `candidates.py`, initially measured on real dev data at the production fold count (10x10=100 folds) as sequential 122.1s vs. parallel 48.4s (2.52x speedup), with all 100 `pr_auc_scores` bit-identical between runs. A same-setup re-measurement the next day did not reproduce that gain — sequential 105.0s vs. parallel 83.4s, only 1.26x — while per-fold `train_time_s` became contention-noisy (1.0s sequential vs. 4-8s parallel, varying run to run) and no longer comparable to `dummy_prior`/`lgbm_default`'s sequential timings. Not worth it at the reproduced magnitude — reverted to sequential for all three candidates; `_LOGREG_CV_N_JOBS` and the per-candidate `n_jobs` dict removed from `candidates.py`. `tests/unit/test_train_candidates.py` (5 tests) still passes.
- [x] `CLAUDE.md` corrections that describe PR A's code land in this PR (see "Doc corrections" section below for the full list — split by which PR's code they describe). **Done 2026-08-08**: of the four "Doc corrections" bullets, only one describes PR A's own code (item 148 below — the `test_architecture.py` enforcement pointer); the other three (147, 149, 150) describe B0/B1/B2-era or unrelated code and stay unchecked, correctly out of scope here. Landed: the bare-relative-paths rule gains a new sibling bullet stating the `activate_config`/`compose_config` pairing rule PR A introduces, with the `test_every_compose_config_call_site_also_calls_activate_config` enforcement pointer inline (no prior rule existed to retrofit a pointer onto, since `activate_config` didn't exist before this PR); the `datasets/processed/telco_churn_features.parquet` rule gains the `test_processed_data_path_is_read_only_by_its_canonical_resolvers` pointer. Separately, the `training_manifest.json` spec line's "DVC data hash" wording (stale per this PR's `data_content_hash` fix above) corrected to describe the real `features_sha256()`-sourced hash and explain why it's DVC-independent.

## PR B0 — Resolve by run id, not registry version *(hard precondition for B1; purely additive)*

**PR B0 complete (2026-08-08).** All eight items below landed together. New `utils/mlflow.py` machinery: `resolve_model_version_from_run_id` (the reverse of `resolve_model_run_id`, via `search_model_versions`), `resolve_model_identifier(explicit_run_id, explicit_model_version, cfg) -> (run_id, model_version, model_uri)` (raises on both given; resolves from `reports/calibrate_receipt.json` when neither is), and the receipt read/write pairs. `configs/{threshold,evaluate,error_analysis}/default.yaml` each gained a `run_id: null` key beside `model_version: null`. Verified: `make test-models`-equivalent unit slice (`mlflow`/`calibrate`/`threshold`/`evaluate`/`error_analysis`/`train_log_model`, 239 tests) green, plus the full unit suite (899 tests, 96.42% cov), `mypy --strict`/`ruff check` clean on all of `src/telco_churn`. All five subprocess integration suites (`train`, `threshold`, `evaluate`, `error_analysis`, `register` — 18 tests, real Docker Postgres+MLflow) re-run green after fixing a real leak `test_train_subprocess.py` had (see Fixed, `CHANGELOG.md`). A real `uv run python -m telco_churn.models.train` run confirmed the receipt chain works end to end outside of tests; `notebooks/03c-hyperparameter-tuning.ipynb` re-executed against that run, byte-identical numbers to the prior run.

- [x] `calibrate.py`, `threshold.py`, `evaluate.py`, `error_analysis.py` resolve their model from the upstream step's receipt when no explicit identifier is passed; error if both an explicit run id *and* an explicit version are passed — never silently prefer one. `calibrate.py` is the one exception the "both" clause doesn't apply to: it has a single identifier axis (`run_id`, resolved from `train_receipt.json`), since nothing is registered yet at that point in the pipeline.
- [x] `models/train/` and `calibrate.py` write those receipts. `log_model.py::run_model_logging_step` writes `reports/train_receipt.json` (`{"run_id"}`); `calibrate.py::run_calibration_step` writes `reports/calibrate_receipt.json` (`{"run_id", "model_version", "logged_model_id", "model_uri"}`), both right before their final `logger.info`/return.
- [x] `load_fitted_model` takes the receipt's `model_uri` on the receipt path, keeps the registry URI on the explicit-version path (`evaluate.py`, `error_analysis.py`). Signature changed from `(model_version, cfg)` to `(model_uri, cfg)`; every caller (`_load_and_score_candidate`, `_load_error_analysis_inputs`, `load_incumbent_proba`) now builds or threads the URI itself.
- [x] `threshold_validation.json` model stamp: `model_version` → `logged_model_id` (`threshold.py::_assemble_threshold_payloads`); `check_threshold_provenance` (`evaluate.py`) compares on `logged_model_id`; `error_analysis.py` updated; `model_run_id` kept in both payloads as a locator only, no longer load-bearing.
- [x] `screen_passed` hardening: moved into `validation_payload` (model-dependent half), not the policy payload; new `check_threshold_screen_passed(validation_payload)` beside `check_threshold_provenance`, called unconditionally at both downstream readers; no override flag.
- [x] `test_predictions.parquet`: stamped with the `logged_model_id` `evaluate.py` evaluated (a constant column, broadcast every row); `error_analysis.py` checks it against its own resolved id and raises `ValueError` naming both on mismatch (closes the stale-copy-on-hand-run-rollback hole).
- [x] `error_analysis.py`'s dev-OOF carry-through: `_load_dev_oof_diagnostics_carrythrough` deleted outright; its call site now uses the canonical `load_dev_oof_diagnostics(run_id, cfg)` (fetches threshold.py's own `dev_oof_diagnostics.json` via MLflow, by run_id) — `reports/metrics.json` is no longer read by this module at all.
- [x] Fixed the `.get(key, [])` encoding defect: the four `v1_flagged`/`v2_equal_opportunity_flagged`/`v2_demographic_parity_flagged`/`v2b_flagged` keys are now indexed directly off `dev_oof_diagnostics` (raise `KeyError` if missing this cycle, since the swap above means the dict is always a live fetch); a `_NOT_ASSESSED = "not_assessed"` sentinel constant documents the rule for a future key that postdates an older artifact version — unexercised today since none of the four need it.

## PR B1 — Promotion-decision integrity *(after B0, before B2)*

**PR B1 complete (2026-08-09).** All eight items below landed together, plus the previously-unaccounted-for "review CLI" — PROJECT_PLAN.md's registration/review resolution names it as its own Jupyter-independent entry point (option 2), distinct from `register.py`'s CLI, and it did not exist on this branch — so this PR also adds new `src/telco_churn/models/review.py` + `configs/review/default.yaml`. Verified: full unit suite (910 tests, 96.44% cov, `models/review.py` at 100%), `ruff check`/`ruff format --check`/`mypy --strict` clean on all of `src/telco_churn`, and all affected subprocess integration suites (`test_register_subprocess.py`, `test_evaluate_subprocess.py`, `test_error_analysis_subprocess.py`, `test_calibrate_subprocess.py`, new `test_review_subprocess.py` — 23 tests total) green against real Docker Postgres+MLflow.

- [x] Split `promotion_decision.json` (machine: `{regime, gate, criteria, model_version, metrics_content_hash}`) from `promotion_review.json` (human, append-only `entries` list: `{verdict, notes, approver, reviewed_at}`), logged onto the eval run. **Done**: `promotion_review.json` also carries top-level `eval_run_id`/`metrics_content_hash` (binding it to a specific evaluation without needing `model_version`, which doesn't exist yet at review time — register.py mints it only after review, on a pass), per PROJECT_PLAN.md's resolved design.
- [x] `decide_promotion` drops the `review` field entirely (`gate.py:341-346`). **Done.**
- [x] Route human rejection through `_tag_rejected`, same as the other 3 reject sites *(confirmed live defect today: a human rejection is left at `pending`, indistinguishable from a crash artifact)*. **Done** — new `"human_rejected"` reason in `_rejection_description`/`_tag_rejected`, checked before the gate-fail branch (mirrors the pre-B1 ordering of the review check).
- [x] `register.py`'s `_check_review_approval` reads `entries[-1]` from `promotion_review.json`, not the decision dict. **Done** — replaced by `_resolve_review_verdict(eval_run_id)` + inline branching in `run_registration_step` (approved/rejected/pending/unrecognized).
- [x] Delete `direction_sanity_check_fired` entirely *(confirmed still present, `gate.py:355-378`)* — superseded by `error_analysis_run_id` already read from the model-version tag. **Done.**
- [x] Extract calibrate.py's registration call into `register.py` *(confirmed `calibrate.py:1340-1408` still performs registration itself via `_point_challenger_alias`)*. **Done**: new `register.register_challenger(cfg, model_info, input_example, in_memory_preds) -> version` (mint, tag pending, verify parity, point `challenger`), called from `calibrate.py`'s `run_calibration_step` via a function-local import — a top-level import would be circular, since `register.py` already imports `calibrate.py`'s manifest/dev-features loaders at module level (same shape as `utils/mlflow.py::load_model_promotion_bars`'s existing circular-import workaround).
- [x] Move the six model-version tags currently written by `evaluate.py:1777-1808` and `error_analysis.py:1809` into `register.py`, sourced via `reports/eval_receipt.json` / `reports/error_analysis_receipt.json`. **Done**: new `_resolve_and_tag_eval_run_id`/`_tag_gate_criteria`/`_resolve_and_tag_error_analysis_run_id` in `register.py`, resolving from the model-version tag if a prior pass already wrote it, else from the two new receipts (`utils/mlflow.py::write/read_eval_receipt`, `write/read_error_analysis_receipt`) — tagged immediately so every later read goes through the tag. `evaluate.py`/`error_analysis.py` write the receipts and no longer call `set_model_version_tag` at all.
- [x] `CLAUDE.md`: amend the "Phase 6 `calibrate.py` performs the single registration" rule + the Phase 6/7 Verification-line amendments this PR falsifies. **Done**: `CLAUDE.md`'s registry section rewritten (registration-boundary rule, `review`-field removal, tag-ownership move naming `register.py` as the sole tag writer on every version it processes, `model_card.json`'s `error_analysis_run_id` provenance line); `PROJECT_PLAN.md`'s Phase 7 Verification line updated to describe the receipt/tag/review-CLI flow. `docs/architecture.md`'s registration-flow diagram and prose also updated (found stale during this pass, not separately tracked — same footing as a Verification-line amendment).
- [x] Update `configs/calibration/default.yaml`, `configs/register/default.yaml` *(⚠ already edited on branch, uncommitted — `require_review` stays `true`, Phase 10's value explicitly left open per the new comment)*, the review CLI, notebook 05. **Done**: both configs confirmed already correct (no further edit needed); new `configs/review/default.yaml`; notebook 05's closing cell rewritten for the new `record_review` signature (fetches any existing `promotion_review.json` off the eval run, appends, re-logs — never rewrites `promotion_decision.json`), imports gain `mlflow.artifacts`.
  - **Follow-up (2026-08-09): the notebook's closing cell is removed outright, not merely rewritten.** Once `review.py` existed as a Jupyter-independent path, keeping `record_review` callable from two places (the CLI and the notebook) was the exact "two independent evaluations of one rule... becomes two rules" pattern this project avoids elsewhere (`CLAUDE.md`'s gate-decision rule makes the same argument). `notebooks/05-evaluation-and-error-analysis.ipynb`'s `## 8. Human review — model risk sign-off` section (markdown framing + the `record_review` code cell) is deleted; `models/review.py` is now the sole producer of `promotion_review.json`. The notebook's imports cell drops the now-dead `record_review`, `mlflow.tracking`, `mlflow.artifacts`, and `datetime`/`timezone` imports; the closing `## Summary` cell's "verdict recorded below" line is reworded to point at `models/review.py`. Docs/tests updated to match: `CLAUDE.md`, `docs/architecture.md`'s registration diagram, `register.py`'s `require_review` error message, `gate.py::record_review`'s docstring, `configs/register/default.yaml`'s comment, `README.md`'s quick-start (step 8 now runs `models.review` before `make register`), `ANALYSIS.md` §7's notebook pointer, `PROJECT_PLAN.md`'s structure table/critical-files table/Phase 7 Verification line, and the two stale "mirrors notebook 05's closing cell" test comments in `tests/integration/test_register_subprocess.py`.
  - **Follow-up (2026-08-09): `review.py` itself was a guided-only-by-error CLI — closed the gap.** Deleting the notebook's closing cell means nothing renders diagnostics for a reviewer anymore unless they already know to look elsewhere, and the CLI's required fields were only discoverable by triggering each `ValueError` in turn. New `_print_review_guidance` (`review.py`'s `__main__`) logs, unconditionally and before any validation, where this cycle's diagnostics actually live and what `review.verdict`/`review.approver`/`review.notes` each expect. Pointed at the MLflow run (`metrics.json`/`economics.json`/`promotion_decision.json`/`error_analysis.json`/`figures/`, all under `eval_run_id`) rather than the notebook or the local `reports/` mirror, after two rounds of correction: the notebook is only accurate if someone re-executed it against this exact `eval_run_id`, which nothing enforces, and `reports/` is gitignored per-machine scratch (`.gitignore:229`) — neither is reachable by a reviewer who isn't the same person, on the same machine, that ran `evaluate.py`/`error_analysis.py`. The MLflow run is the one artifact guaranteed reachable by anyone pointed at the shared tracking URI, since the server proxies artifact reads through its own API (`docker-compose.yml`'s `--artifacts-destination`) regardless of which machine produced them. Separately, `notes` moved from an optional, blank-by-default field (`configs/review/default.yaml`: `notes: ""`) to hard-required and non-blank (`notes: null` + a new `_require_notes` validator) — an empty note defeated the append-only audit trail's purpose as surely as a missing one. New subprocess test `test_review_main_cli_exits_one_when_notes_blank`; `README.md`'s quick-start and `PROJECT_PLAN.md`'s Phase 7 Verification line updated for the now-required `review.notes=...` argument.

## PR B2 — V3 pre-seal screen *(after B1; has a mandatory internal sequence — see below)*

**PR B2 complete (2026-08-09).** All items below landed in the sequence the mandatory internal ordering required: `calibrate.py`'s dev-SHAP logging landed and was run for real against Docker Postgres+MLflow (a fresh `tuning_study` → `calibrate` cycle, model version 1), the resulting `dev_shap_summary.json` ranking was inspected by hand, `v3_top_k_features=8`/`v3_min_direction_magnitude=0.3` were derived from it (confirmed by the user before the binding screen was written), and only then did the binding screen land. Verified: full unit suite green (`ruff check`/`ruff format --check`/`mypy --strict` clean on all of `src/telco_churn`), `test_calibrate.py`/`test_threshold.py`/`test_error_analysis.py`/`test_evaluate.py`/`test_register.py`/`test_explain.py`/`test_shap_values.py` all pass, including new dedicated tests for `_run_v3_direction_sanity`'s pass/fail/weak/zero-checked mechanics and for the veto-set pinning (item 3, below). Docker-based integration test fixtures (`test_error_analysis_subprocess.py`, `test_evaluate_subprocess.py`, `test_register_subprocess.py`, `test_threshold_subprocess.py`) were updated by static analysis (a `threshold.v3_top_k_features=3` override, matching each fixture's synthetic data's three real signal columns) but not re-run against Docker in this session — worth a real integration run before merging.

- [x] Move V3 (direction-sanity check) into `threshold.py`'s pre-seal screen, binding alongside the calibration slope
- [x] `screen_passed` becomes `failures: list[dict]` (`{criterion, detail, remediation}`) instead of a bool; exactly two append sites (`calibration_slope`, `v3_direction_sanity`); **do not** fold V1/V2/V2b into the veto set — they stay reported-only per `CLAUDE.md`'s three-guardrail rule
- [x] Test pinning the veto set: build a payload with V1/V2/V2b all flagged, slope + V3 passing → assert `screen_passed is True`. **Done**: `test_run_dev_oof_screen_ignores_v1_v2_v2b_flags_in_failures` (`test_threshold.py`) — `compute_dev_oof_diagnostics` mocked to report every one of V1/V2/V2b flagged, slope+V3 passing, asserts `failures == []`.
- [x] `register.py` backstop (~3 lines) for the out-of-band manual `evaluate` → `register` path. **Done**: `check_threshold_screen_passed(load_threshold_validation(run_id, cfg))`, called in `run_registration_step` right after `run_id` is resolved, before `_check_manifest_artifacts`.
- [x] New `models/shap_values.py` (~25 lines) — move `_shap_values_for_test_rows` out of `error_analysis.py`, renamed off "test"; unwrap `CalibratedClassifierCV` separately from the compute. **Done**: `unwrap_calibrated_pipeline`/`compute_shap_values`, both used by `calibrate.py` (dev) and `error_analysis.py` (test).
- [x] `explain.py` gains `EXPECTED_EDA_DIRECTIONS`, `direction_sanity_check`, `_check_top_k_elbow` — pure, no `shap` import. **Done**, with one deliberate deviation from the literal name: `_check_top_k_elbow` is exported **public** as `check_top_k_elbow` (no underscore) — it's now called cross-module by `threshold.py` and `error_analysis.py`, and every other cross-module import in this codebase is public; an underscore-prefixed cross-module import would be the first of its kind. Also gained `feature_directions` (not in the original bullet list) — `calibrate.py`'s dev-SHAP summary needs a direction for *every* feature, not just a top-k slice, and `dependence_points` would additionally build and discard the full paired-points lists to get it.
- [x] `calibrate.py` logs `dev_shap_values.parquet` + per-feature `(mean_abs_shap, direction)` for every transformed feature. **Done**: `_compute_dev_shap`, logged as `calibration/dev_shap_values.parquet` (full matrix) + `calibration/dev_shap_summary.json` (the summary, sorted by `mean_abs_shap` descending).
- [x] `threshold.py` ranks/cuts at `v3_top_k_features`, calls `direction_sanity_check` — no `shap` import in `threshold.py`. **Done**: `_run_v3_direction_sanity`.
- [x] `configs/threshold/default.yaml` gains `v3_top_k_features`, `v3_min_direction_magnitude` *(confirmed both absent today)* — derive from the **dev** ranking, not carried over from the test-derived `8`. **Done**: `v3_top_k_features: 8` (elbow at rank 8→9, ratio ≈4.7×, matching the independently-derived test-side `top_k_dependence_features=8`), `v3_min_direction_magnitude: 0.3` (no effect on the derivation run's actual top-8, all ≥0.87 — a forward-looking guard against a future weak/unstable feature).
- [x] `configs/error_analysis/default.yaml`: rename `top_k_shap_features` → `top_k_dependence_features` *(confirmed old key still present, line 37)*, decoupled from V3
- [x] `error_analysis.py` re-audits the **same** feature set `threshold.py` derived on dev, via `runs:/<run_id>/threshold/dev_oof_diagnostics.json` — **not** via the `reports/`-path helper; no independent test-side `k`. **Done**: `dev_oof_diagnostics` (via `evaluate.load_dev_oof_diagnostics(run_id, cfg)`) is now loaded *before* `_compute_shap_diagnostics`, and its `v3_top_k_feature_names` threads straight into `direction_sanity_check_test`.
- [x] Test dependence grid renders `top_k_dependence_features` ∪ the inherited dev audit set, sorted by test mean-|SHAP|, panel titles marked audit-set-member / direction-contradicts. **Done**: `_compute_shap_diagnostics`'s `dependence_feature_set`; `_save_dependence_plots` marks `[audit-set]`/`[CONTRADICTS]`; `test_run_error_analysis_step_dependence_grid_includes_dev_v3_audit_set` pins the union/sort.
- [x] Surface `checked_count`/`weak_count` in `dev_oof_diagnostics.json`; zero-checked counts as a screen failure. **Done**: `v3_checked_count`/`v3_weak_count`; a zero-checked V3 folds into the single `v3_direction_sanity` failure-append site (not a third site — see the "exactly two append sites" item above).
- [x] `_check_top_k_elbow` runs on both cutoffs (dev-side guarding `v3_top_k_features`, test-side guarding `top_k_dependence_features`); warn-never-raise on both. **Done**: `threshold.py::_run_v3_direction_sanity` (dev), `error_analysis.py::_compute_shap_diagnostics` (test) — both log a warning event, never raise.
- [x] Rename result keys: `v3_direction_sanity` (binding, pre-seal) vs. `direction_sanity_check_test` (reported, `error_analysis.json`); update `register.py:1022`. **Done**: `register.py`'s model-card `v3_direction_sanity_violations` now reads `dev_oof_diagnostics_carried_through.v3_direction_sanity` (the binding verdict), not `error_analysis.json`'s own `direction_sanity_check_test` (the reported-only re-audit).
- [x] **Mandatory internal sequence — must run in this order within B2:** (1) land `calibrate.py`'s SHAP logging → (2) run it → (3) inspect the dev ranking → (4) derive `v3_top_k_features` and `v3_min_direction_magnitude` from that ranking → (5) land the binding screen. **Followed exactly** — see the PR-level status note above.
- [x] Fail loudly (name the cause) if `dev_shap_values.parquet` is absent on a hand-run older model version — never silently skip V3. **Done**: `threshold.py::load_dev_shap_summary` catches `MlflowException` on a missing `dev_shap_summary.json` and re-raises `RuntimeError` naming the run and the likely cause (a hand-run older model version predating dev-SHAP logging).
- [ ] Phase 14 runbook note: a rollback does not require re-deriving `t*` (the policy file is model-independent) — do not re-run `calibrate` on an old version "just to be safe". **Not actioned** — `docs/runbook.md` doesn't exist yet (Phase 14 hasn't started); the content is already captured in `PROJECT_PLAN.md`'s Phase 8 planning prose (the `⚠ A rollback needs no threshold action at all` paragraph) for Phase 14 to pick up when the runbook is written.

## PR C — Stage-entry-point extraction *(lands last, after B1 and B2)*

- [ ] New `models/calibration_metrics.py`: `pooled_brier`, `brier_skill_score`, `expected_calibration_error`, `murphy_decomposition`, `calibration_slope` — strip `cfg` params, take `n_bins`/`strategy` directly *(confirmed 2 of 5 still take `cfg` today)*
- [ ] New `models/artifacts.py`: `load_training_manifest`, `committed_features_from_manifest`, `unfitted_pipeline_from_manifest`, `load_dev_features`, `load_dev_customer_ids`, `load_dev_oof_predictions`, `load_fitted_model`, `resolve_champion_version`, `load_threshold_validation`
- [ ] New `models/policy_config.py`: `CostScenario`, `load_costs_config`, `costs_config_hash`, `load_policy_thresholds`, `resolve_policy_scenarios`, `resolve_policy_thresholds_by_scenario`, `load_model_promotion_bars` (moved from `utils/mlflow.py:184` — *confirmed currently undeclared in any stage's `deps:`*)
- [ ] `content_hash` (`evaluate.py:906`) → `utils/`
- [ ] `check_threshold_provenance` (`evaluate.py:249`) → `gate.py` (pure, stdlib-only — do not park I/O-bearing moves here)
- [ ] `register.py:47`'s imports fully rehomed — completeness test: no module under `src/` imports a `__main__`-bearing module
- [ ] `test_architecture.py` guard: no module under `src/` may import from a module containing a `__main__` block *(confirmed not among the current 16 tests)*

---

## Doc corrections (land alongside the PR whose code they describe — never ahead of it)

- [ ] `CLAUDE.md`: rewrite the "test set touched once" invariant — restate as "only `evaluate.py` may bind the test half of `partition()`" (current wording is literally false: `error_analysis.py:1196-1913` references `y_test`, though it is compliant in substance)
- [ ] `CLAUDE.md`: add "— enforced by `tests/unit/test_architecture.py`" pointer to every rule now covered by a guard *(partially done 2026-08-08, as part of PR A's own doc-corrections item above: the two guards that exist today — `test_every_compose_config_call_site_also_calls_activate_config`, `test_processed_data_path_is_read_only_by_its_canonical_resolvers` — both have pointers. Left unchecked because the bullet's scope is every guard, and B0/B1/B2/C each add more (e.g. PR C's "no module imports a `__main__`-bearing module" guard); re-visit as each lands.)*
- [ ] `CLAUDE.md`: define "public" for the `__all__` rule (5 files legitimately lack it: `serving/`, `ui/`, `monitoring/` placeholders, `utils/__init__.py`, root `__init__.py`)
- [ ] `CLAUDE.md`: clarify "random state is always 42" — `src/` has zero hardcoded literals, every call site reads config
- [x] `pyproject.toml` ruff: add `TRY`, `LOG`, `PTH` to `select`; resolve the dead `per-file-ignores` `ANN` entry (`ANN` isn't in `select`). **Done 2026-08-07**: `select` gains `TRY`/`LOG`/`PTH`. Enabling them cold surfaced 73 violations (56 `TRY003` raise-vanilla-args, 12 `PTH123` builtin-open, 5 `TRY301` raise-within-try) — `TRY003`/`PTH123` ignored (bulk pre-existing patterns, not worth a same-day mass refactor of exception classes and `open()` call sites); the 5 `TRY301` sites fixed directly (`calibrate.py`, `error_analysis.py`, `evaluate.py`, `register.py`, `threshold.py` — each `__main__` block's CLI-arg validation moved into a small `_require_*(cfg) -> str` inner function so the `raise` no longer sits inside the `try`). Dead `per-file-ignores` `ANN` entry: resolved by *not* adding `ANN` to `select` — enabling it separately surfaced ~50 more violations (`ANN401` on `Any`-typed config params in `utils/paths.py` and elsewhere) that are a distinct, larger scope than this item; the dead ignore line was left as-is rather than removed, since re-adding `ANN` to `select` in a future round makes it live again. `ruff check src/ tests/` clean; `mypy --strict` clean on all touched files.
- [x] `CLAUDE.md`: `training_manifest.json` spec line's "the paired-Δ vs LogReg with its full bootstrap CI" corrected to describe the `model_family_committed` reference section — **done 2026-08-07, landed with the model-family retirement's own code** (same PR, not deferred, since the rule the old wording stated is what the retirement changes).

## Verification (from the plan's own Verification line)

- [ ] `make test-features` / `make test-models` pass with the selection wiring removed
- [ ] `uv run python -m telco_churn.models.train` completes with no `feature_selection` CV; logs the permutation-importance table, SHAP audit, and a non-null demographic joint-importance value
- [ ] `uv run pytest tests/unit/test_architecture.py` passes, including the new `compose_config`-implies-`activate_config` test
- [ ] Integration suite passes with every `PROCESSED_DATA_DIR` occurrence replaced (`uv run pytest tests/integration/ --run-integration --no-cov`)
- [ ] `oc.env:PROCESSED_DATA_DIR` survives nowhere in `configs/`
- [ ] Discriminating check: export `PROCESSED_DATA_DIR` to a stale directory, confirm every entry point behaves exactly as with the variable unset

---

## PR split (reference)

| PR | Subject | Depends on |
|---|---|---|
| A | Data paths, config literal, `activate_config`, selection retirement, training fixes | §0 pre-work (for retirement only) |
| B0 | Resolve by run id, not registry version | A |
| B1 | Promotion-decision integrity (review split, registration extraction) | B0 |
| B2 | V3 pre-seal screen | B1 |
| C | Stage-entry-point extraction | B1 + B2 |

Audited against the codebase 2026-08-06 — no false or stale claims found in the plan text itself; all "not yet done" items above were confirmed genuinely not done.
