# Phase 12a — Minimal Cloud Deploy: To-Do

Working checklist for implementation. Full rationale, corrections, and decision history for every item here lives in `PROJECT_PLAN.md` § *Phase 12a* — this file is the trimmed, checkable version for day-to-day tracking. When in doubt about *why* an item is written the way it is, go read the matching bullet there before changing it here.

**Goal:** v1 genuinely deployed — public HTTPS API + UI on AWS, auth on, up 24/7, no laptop in the loop. Runs after Phase 10a, before Phase 10b.

**Cost reality (keep this visible, don't let it drift back to "free"):** ~$28–30/mo, metered against this account's shared Free Plan credit balance ($155.32 remaining as of 2026-09-16, expiring 2027-01-16) — **not** the classic 12-month free tier, which this account does not have active for any of these resource types. App Runner/ECS-Fargate and RDS multi-AZ are **not built** anywhere in 12a or 12b — they're a documented "if this needed to scale" recommendation in `README.md` (Phase 12b), because they cost *more* than this setup, not less.

---

## Group 0 — Account prerequisites (one-time, console + laptop)

- [x] MFA on root; recovery codes stored offline; stop using root after setup
- [x] Not IAM Identity Center — Organizations-based enablement forfeits Free Plan credits, and an account instance can't grant AWS account access (permission sets unsupported). See `PROJECT_PLAN.md` Group 0 for the full reasoning.
- [x] IAM user `telco-churn-admin` (MFA enabled, no direct permissions except `sts:AssumeRole`) + IAM role `terraform-admin` (`AdministratorAccess`, trust scoped to the user + `aws:MultiFactorAuthPresent`) — verified via `aws sts get-caller-identity --profile telco-churn` returning `assumed-role/terraform-admin/...` after an MFA prompt
- [x] AWS CLI v2 (2.36.36) + Terraform (1.16.2) installed locally; `~/.aws/config` two-profile chain (`telco-churn` assumes `terraform-admin` via MFA) — no usable long-lived admin credential on the laptop
- [x] AWS Session Manager plugin installed locally (needed for Group 8 shell access and the Group 6.5 RDS tunnel)
- [x] Region fixed to `us-east-1` for every resource this project creates
- [x] Confirm current credit balance in Billing → Credits — **$155.32 estimated remaining, $4.68 used, $160.00 total** confirmed 2026-09-16, matches TODO's recorded figures. Expiry date still unconfirmed on-screen — assumed 2027-01-16 per earlier confirmation, worth a second look next time the Credits page is open.
- [x] CloudWatch billing alerts toggle flipped to "Delivered" (Billing preferences → Alert preferences), confirmed 2026-09-16 — required before any `EstimatedCharges` CloudWatch alarm can receive data
- [ ] Billing alarms at `EstimatedCharges > $100` and `> $140`, **plus** an AWS Budgets budget at the same thresholds with 50/80/100% alerts — before creating any billable resource. (These only matter if/when the account is upgraded to a Paid Plan — on Free Plan, spend stops when access ends, not at a dollar threshold.) **Held for Group 1's Terraform scaffold, per 2026-09-16 decision — not created manually in console.**
- [x] **Second calendar reminder, well before 2027-01-16 (set for 2026-12-31):** explicit decision — upgrade to Paid Plan, or run Group 12's teardown ahead of the cutoff. This is the harder deadline; don't let it get buried under the classic-free-tier reminder in Group 12, which is a different, softer clock.
- [ ] **Standing habit, not a one-time task — applies only during the Groups 1–11 build/test window, retired once Final Verification confirms genuine 24/7 uptime (per the always-on decision above):** `stop` (not terminate) EC2 when not actively developing — near-$0 compute cost while stopped vs. ~$25/mo running
- [x] **Decided 2026-09-16: genuine 24/7 uptime, ~$28–30/mo.** The API/UI link goes on the CV and GitHub profile, reachable by a recruiter at any time with no demo window to schedule around — "up on demand" would mean the link is broken most of the time it's actually clicked. Cost-minimization levers already evaluated and ruled out: Graviton/ARM instances, self-hosted Postgres-on-the-box instead of RDS (both declined 2026-09-16 — see `PROJECT_PLAN.md` for the numbers). Do **not** cut the Elastic IP (~$3.65/mo) — Caddy/DuckDNS need the stable IP.
- [x] Free DuckDNS domain reserved (`telco-churn.duckdns.org`, signed in via GitHub); token saved for Group 8's DNS update

## Group 1 — Terraform scaffold + remote state

- [x] State backend bootstrapped: `infra/bootstrap/main.tf` created `telco-churn-tfstate-742306314937` (versioning + SSE + Block Public Access), applied 2026-09-16. Lock is S3 native (`use_lockfile = true` in `infra/versions.tf`'s backend block, Terraform 1.10+) — no DynamoDB table needed.
- [x] `infra/` root module: `versions.tf`, `providers.tf` (region + `default_tags`: Project/Environment/ManagedBy), `variables.tf`, `outputs.tf` created
- [x] `terraform init` against the S3 backend — succeeded, confirmed with a clean `terraform plan` ("No changes"). `.terraform.lock.hcl` ready to commit; `*.tfstate*`, `.terraform/`, secret `*.tfvars` gitignored
- [x] Per-concern file split: `network.tf`, `iam.tf`, `compute.tf`, `data.tf`, `ecr.tf`, `ssm.tf`, `monitoring.tf` scaffolded (comment-only placeholders, filled in as each later group lands)
- [x] `infra/scripts/assume-admin.sh` — MFA workaround for Terraform's AWS provider, which can't prompt for MFA itself; source before every `terraform` command to export short-lived session credentials

## Group 2 — IAM

- [x] EC2 instance role + instance profile (trust: `ec2.amazonaws.com`)
- [x] Instance permission policy: ECR auth+pull (two repos), `s3:GetObject`/`PutObject` (artifact bucket), `ssm:GetParameter*` (`/telco-churn/*`), `kms:Decrypt` (`aws/ssm` key), CloudWatch-agent actions; Session Manager + Run Command via the AWS-managed `AmazonSSMManagedInstanceCore` policy (attached, not hand-rolled — least-privilege via AWS's own purpose-built policy rather than risking a silently-broken shell path)
- [x] GitHub OIDC provider (`token.actions.githubusercontent.com`) — created, thumbprint accepted on apply
- [x] CI deploy role (`telco-churn-ci-deploy`): trust scoped to `repo:Ampofowaa/TelcoChurn_PortfolioProject:ref:refs/heads/main`; permissions = ECR push (two repos) + `ssm:SendCommand` **scoped by the `Project` resource tag, not an instance ARN** + `ssm:GetCommandInvocation`
- [x] ECR/S3 ARNs written as interpolated string literals (account ID resolved via `data.aws_caller_identity`, not hardcoded) — those resources don't exist yet either
- [x] `terraform apply` — 7 resources added, 0 changed/destroyed. Grepped `iam.tf` for stray wildcards: exactly 4 `Resource: "*"` hits, all AWS-required (no ARN-level scoping exists for `ecr:GetAuthorizationToken` ×2, `cloudwatch:PutMetricData`, `ssm:GetCommandInvocation`); zero `iam:*` anywhere

## Group 3 — Networking (VPC)

- [x] `aws_vpc` `10.0.0.0/16` (`vpc-06b8e9ce87d32480f`), DNS hostnames + support enabled
- [x] Two public + two private subnets, two AZs
- [x] Internet gateway + public route table; private route table (local only)
- [x] SG **ec2** (`sg-04c9820f07c2d91a4`): in `80`+`443` from `0.0.0.0/0`, out all — **no inbound 22** (shell is SSM Session Manager only)
- [x] SG **rds** (`sg-0fb8fccba914cdaf3`): in `5432` from the ec2 SG only
- [x] S3 gateway VPC endpoint on the route tables
- [x] `aws_eip` reserved for the instance (`100.59.128.63`)
- [x] `terraform apply` — 16 resources added (hit one issue: EC2 `GroupDescription` rejects non-ASCII, an em dash in the ec2 SG description broke the first apply — fixed, ASCII-only now, second apply picked up the remaining 2 resources cleanly)

## Group 4 — S3 (MLflow artifact store)

- [x] `aws_s3_bucket` `telco-churn-mlflow-742306314937` (account-ID suffix for global uniqueness — plan's `telco-churn-mlflow` literal wasn't guaranteed available; `iam.tf`'s `mlflow_bucket_arn` updated to match), `force_destroy = true`
- [x] Public-access-block (all four true); SSE-S3; versioning on
- [x] Lifecycle: expire noncurrent versions 30d; abort incomplete multipart uploads 7d
- [x] Bucket policy: deny non-TLS; allow only the instance-role ARN
- [x] `terraform apply` — 6 added, 1 changed (EC2 instance role's inline policy, repointed at the new bucket ARN). Anonymous access confirmed `403` via `curl`; instance-role list access can't be verified until Group 8's EC2 box exists — revisit then
- [x] Note for later: inspect contents via the MLflow UI only, never the raw S3 console (opaque MLflow-managed IDs)

## Group 5 — ECR

- [x] `aws_ecr_repository` × 2 (`telco-churn/api`, `telco-churn/ui`): `scan_on_push = true`, `image_tag_mutability = IMMUTABLE`, `force_delete = true`
- [x] Lifecycle policy on each: expire untagged after 7d, keep last 5 tagged (`tagPatternList = ["*"]`, not `tagPrefixList` — images are tagged by commit SHA, no fixed prefix to match on)
- [x] `terraform apply` — 4 added (2 repos + 2 lifecycle policies), 0 changed
- [x] One manual build+push per image to prove the path — both tagged `manual-test`, pushed successfully (`api` digest `sha256:5f6242...`, `ui` digest `sha256:ee164d...`), then deleted via `aws ecr batch-delete-image` once confirmed (disposable proof images, not what CD ships — see Group 11 for the real, SHA-tagged automated build)

## Group 6 — RDS

- [x] `aws_db_subnet_group` over the two private subnets (`telco-churn-rds`)
- [x] `aws_db_parameter_group` (`telco-churn-postgres16`, `postgres16` family) with `rds.force_ssl = 1`
- [x] `aws_db_instance`: `postgres`, `db.t3.micro`, `allocated_storage = 20`, **`gp3`** (confirmed 2026-09-16: same $0.115/GB-month as `gp2` in `us-east-1`, but `gp3` gets a free 3,000 IOPS/125 MB/s baseline under 400 GiB — `gp2` would floor at ~60 IOPS on a 20 GB volume for the same price. No reason left to use `gp2`.), `multi_az = false`, `storage_encrypted = true`, `publicly_accessible = false`, `backup_retention_period = 1` (dropped from the plan's original `7` — this account's Free Plan credit restrictions reject anything higher with `FreeTierRestrictionError`, hit 2026-09-16), `deletion_protection = true`, `final_snapshot_identifier` set, master password via `random_password` (not a human-typed `TF_VAR_`, not Secrets Manager)
- [x] `terraform apply` — applied 2026-09-16, `aws_db_instance.main` at `telco-churn-db.cct88is8iujn.us-east-1.rds.amazonaws.com:5432`, `db_name = telco_churn`, `username = telco_admin`
- [x] `CREATE DATABASE mlflow;` — run 2026-09-16 via `psql` (Docker fallback — no native `psql` on this laptop) through the Group 6.5 SSM port-forwarding tunnel (`localhost:5432` → RDS)
- [x] `alembic upgrade head` against the app DB — run 2026-09-16 through the tunnel; all five migrations applied (`customers_raw`, `customers_crm`, `prediction_log`, `prediction_outcomes`, `training_pool`). **Gotcha hit: `localhost` resolved to the IPv6 loopback (`::1`) and hit a different local Postgres server, not the tunnel** (which only binds IPv4) — fixed by using `127.0.0.1` explicitly in `POSTGRES_URL`
- [x] Confirm no manual SQLAlchemy-URL assembly — Group 7's SSM param is itself Terraform-interpolated

## Group 6.5 — Data bootstrap (rebuild fresh, not migrate)

*Runs after Group 8 — needs the EC2 box's SSM tunnel into the private RDS subnet.*

- [x] Tunnel: `aws ssm start-session ... AWS-StartPortForwardingSessionToRemoteHost` → RDS on `localhost:5432`. **Use `127.0.0.1`, not `localhost`, in any `POSTGRES_URL` built against this tunnel** — `localhost` can resolve to the IPv6 loopback on Windows, which the tunnel (IPv4-only) doesn't answer on, silently connecting to some other local Postgres server instead. Also: run every step against this tunnel from your own Git Bash, never via a Claude Code `!`-prefixed command — that runs in a sandboxed environment with no access to the tunnel, your exported env vars, or AWS credentials at all.
- [x] `data/ingest.py` against the tunnel → reproduces `customers_raw` from the committed CSV — confirmed 7043 rows (2026-09-16)
- [x] `make crm-data` against the same connection → regenerates `customers_crm` — confirmed 7043 rows (2026-09-16)
- [x] **Real gap found before running any of this — fixed 2026-09-16:** an experiment's MLflow `artifact_location` is decided once, permanently, the moment it's first created. Locally that default is owned by the running `mlflow server` process (`--default-artifact-root`); against RDS with no server in front of it, the same default would have silently resolved to a local folder on whatever machine ran training first — wrong for a shared backend nothing else can read from. Fixed in code, not a one-off manual step: `configs/config.yaml` gained `mlflow.artifact_location: ${oc.env:MLFLOW_ARTIFACT_LOCATION,}`, and `utils/mlflow.py::ensure_experiment_metadata` now explicitly `create_experiment(..., artifact_location=...)`s on first creation when this is set, never touching an already-existing experiment. Set `MLFLOW_ARTIFACT_LOCATION=s3://telco-churn-mlflow-742306314937/telco-churn-training` alongside `MLFLOW_TRACKING_URI` before the five-step cycle below.
- [x] **Second gap found the same way — fixed 2026-09-16: `boto3` was missing from the dependency tree entirely** (confirmed absent from `uv.lock`, not even transitive). MLflow's S3 artifact repository needs it installed to do anything against an `s3://` artifact_location — without it, `dvc repro --force calibrate` would fail the moment it tried to log the model, and later, `api`/`ui` would fail loading the champion model / `model_card.json` (`ui/streamlit_app.py:911`'s `mlflow.artifacts.load_dict` call) in production. Added `boto3>=1.35` to `pyproject.toml`'s base `dependencies` (not extras-gated — serving, ui, and training all need it) and re-ran `uv sync --all-extras`. **`docker/api/Dockerfile`/`docker/ui/Dockerfile` will need a real rebuild once Group 9/11 actually build them for real** — this changes `uv.lock`, so any previously-built image (e.g. Group 5's disposable `manual-test` proof images, already deleted) predates this and would be missing `boto3`.
- [x] **Reordered 2026-09-17 to match `PROJECT_PLAN.md` (source of truth) — this bullet runs *before* the five-step cycle below, not after.** Re-run `candidates.py`/`comparison.py` (`notebooks/03a-model-selection.ipynb`) and the feature-selection ablation (`notebooks/03b-feature-selection.ipynb`, `RUN_ON_DEMAND_REVIEW` flipped `True` for the run then back to `False` afterward, per its documented steady state) once against this same RDS+S3 backend. **Both completed 2026-09-18, both reconfirmed — no change needed:** `03a` → `lightgbm` reconfirmed (run `3fd5da5b1c674854bcab186ccd4fc7ac`, committed provenance still `c405f6fe...`); `03b` → `full` reconfirmed (run `146379a23a4642aca7fc1692a13aa7bb`, committed provenance still `b918a87e...`). Both S3/RDS round-trips (write via `nbconvert`, read back via MLflow) succeeded — confirms the `artifact_location`/`boto3` fix from below. `COMMITTED_MODEL_FAMILY`/`COMMITTED_FEATURES` left frozen, per the standing rule; both frozen constants' comments plus `ANALYSIS.md` §4a/§4b now note today's run as the AWS-reachable evidence, since the original committed runs predate the AWS migration and won't resolve in the reviewer-facing MLflow UI. Also fixed in passing: a pre-existing stale citation (`de4fac8e`, found not to match the real `COMMITTED_FEATURES_DECISION_RUN_ID`) in `schema.py`, `ANALYSIS.md` §4b, and the notebook itself — corrected to the real value.
- [x] **Incident, 2026-09-18: live RDS master password had drifted from Terraform state/SSM, blocking the `03a` re-run above.** First `nbconvert` attempt against the tunnel failed `FATAL: password authentication failed for user "telco_admin"` — confirmed genuine (not a copy-paste/shell mistake) via a second, independent test: a Docker `postgres:16` `psql` client hitting the same tunnel failed identically. `aws rds describe-db-instances --db-instance-identifier telco-churn-db --query "DBInstances[0].[DBInstanceStatus,PendingModifiedValues,MasterUsername]"` returned `["available", {}, "telco_admin"]`, ruling out a queued-but-unapplied change. Root cause: `aws_db_instance.main` had no `apply_immediately` argument (defaults `false`), so AWS's real behavior on any earlier out-of-band or deferred password write was to queue it for the next maintenance window rather than apply it — and because RDS never returns a master password on read, Terraform/SSM can't detect that kind of drift even in principle (`terraform plan` has nothing to diff against). **Fix:** `infra/data.tf`'s `aws_db_instance.main` now sets `apply_immediately = true` — acceptable here since this is a single-instance box with no production traffic to protect from apply-time blips. Remediation run: `terraform output -raw rds_master_password` → `aws rds modify-db-instance --db-instance-identifier telco-churn-db --master-user-password "<value>" --apply-immediately` → poll for `available` → retest.
- [x] Point `MLFLOW_TRACKING_URI`/`MLFLOW_ARTIFACT_LOCATION` at the new RDS+S3, then run the five-step cycle — **completed 2026-09-18:**
  ```
  dvc repro --force calibrate   # --force required here only — see note below
  register.py
  dvc repro error_analysis      # NOT forced
  models.review
  register.py register.model_version=<n>
  ```
  ⚠ `--force` on the **first** call only — the local `reports/*_receipt.json` files still hash-match against the *old* backend and would otherwise cause `ingest` to be silently skipped. **Never** force the `error_analysis` call — that would re-run `calibrate` a third time and orphan the registry entry `register.py` already minted from the middle run.
  **Result (superseded — see incident below):** minted model version 1 (`make register-challenger`); gate `pass` in `cold_start` regime, all four criteria clear (PR-AUC 0.6658 ≥ 0.6, recall 0.7022 ≥ 0.65, BSS 0.3073 ≥ 0.0, calibration slope 1.0037 in [0.8, 1.25]); human review `approved`; `make register MODEL_VERSION=1` flipped `champion` onto it (first-ever promotion against this backend, so `drift_reference.json`/`model_card.json` were created fresh, not carried over). Two non-gating `evaluate.py` diagnostic warnings surfaced (`cost_scenario_spread_too_narrow`, `capacity_or_budget_exceeded` on `base`/`optimistic`) — logged above for the documentation pass, not blocking.
- [x] **Incident, 2026-09-18: the result above was gated on only 846 of the 1,409 sealed-test rows, not the full set — `ANALYSIS.md` §7's "test set touched once" invariant was silently violated on this very first promotion.** Root cause: `data/split.py`'s `split` stage unconditionally ran `make_reserve()` on every invocation, carving 40% of the test partition into monthly reserve cohorts — a mechanism Phase 10a-ii built for `pipelines/performance_check.py`'s *routine retraining* comparison surface (a bounded stand-in for a rolling holdout), never meant to apply to a cold-start's first-ever evaluation, which should see the untouched sealed set. **Fix:** new `training_setup.reserve_enabled` config flag (`configs/config.yaml`, `dvc.yaml`'s `split` stage `params:`), defaulting `false`; `split.py` gained `make_disabled_reserve()` (reserves nobody, `reserve_month = NULL` for every id) and `sealed_test_ids()`'s defensive assertion now only runs when the flag is on — flip to `true`, deliberately, once Phase 10b's rolling retraining actually begins. **Cleanup required a full hard purge, scoped to the training cycle only (03a/03b's `model_comparison`/`selection_review` runs explicitly protected throughout):** deleted the `telco-churn-pipeline` registered model entity (so version numbering restarts clean at 1, not 2) plus its cold-start cycle's 3 runs (`1505d0a4...` tuning_study/log_model, plus its evaluate/error_analysis runs), the 50 orphaned Optuna trial child runs left behind by that deletion, and a separate failed/uncommitted `model_comparison` retry's 3 support runs — each verified against `03a`'s protected `3fd5da5b...`/`03b`'s protected `146379a2...` before any delete. **In the same pass:** added durable AWS RDS-provenance evidence (`utils/db.py::backend_provenance`/`host_from_url`, new `IngestReceipt` fields, `training_manifest.json`'s `data_backend` section, a `postgres_host` MLflow tag) since the project now assumes all data lives in RDS, not locally; fixed a latent Windows-only crash (`calibrate.py`/`evaluate.py`/`threshold.py`/`error_analysis.py` were missing the `matplotlib.use("Agg")` guard `models/train/__init__.py` already had, so each stage's own subprocess picked an interactive Tk backend and crashed on interpreter shutdown — cosmetic after the fact, but DVC read the non-zero exit as a stage failure); and re-ran `dvc repro --force calibrate` → `register-challenger` → `dvc repro error_analysis` → `models.review` → `register MODEL_VERSION=1` clean against the fixed pipeline. **Corrected result:** `run_id=8550c60c7a544436b792aa8e549de97d`, `eval_run_id=b8fc67ec6870428e9047407f414a7a58`, `error_analysis_run_id=dbbb43a098634a10a667d743e0ab41b2` — full 1,409-row sealed test set (`n_reserved=0`, `n_sealed=1409`), gate `pass` in `cold_start` regime (PR-AUC 0.670 ≥ 0.6, recall 0.698 ≥ 0.65, BSS 0.301 ≥ 0.0, calibration slope 0.992 in [0.8, 1.25]), human review `approved`, `champion` flipped onto the clean version 1. Notebooks `03c`/`04`/`05` re-run and crosschecked line-by-line against these corrected artifacts — no discrepancies found.
- [x] Confirm `resolve_champion_model` loads the new champion before pointing any traffic at the box — **confirmed 2026-09-18** (re-confirmed after the incident above, against the corrected `run_id`): loaded `model_version=1`, `run_id=8550c60c7a544436b792aa8e549de97d`, `committed_features=20` (matches `03b`'s reconfirmed `full` set), no exception raised.
- [x] No separate step for Optuna's `optuna` Postgres schema — RDS has no `docker-entrypoint-initdb.d` equivalent, so `sql/schema/002_create_optuna_schema.sql` only gets applied here via `tuning.py::_build_optuna_storage`'s own direct-execution path, as a side effect of the `dvc repro --force calibrate` call above (which reruns `train` upstream of `calibrate`). Confirmed working — no schema-missing error surfaced during the five-step cycle above.

## Group 7 — SSM Parameter Store

- [x] `/telco-churn/api-key` (SecureString, `openssl rand -hex 32`) — human-set, `ignore_changes`. Terraform resource applied 2026-09-16 with placeholder value `"REPLACE_ME"` — **still pending: run** `aws ssm put-parameter --name /telco-churn/api-key --type SecureString --value "$(openssl rand -hex 32)" --overwrite` **to set the real value** (harmless to do any time before Group 9 needs it; not blocked on anything)
- [x] `/telco-churn/grafana-endpoint`, `/telco-churn/grafana-api-key` (SecureString) — human-set from Grafana Cloud console, `ignore_changes`. Terraform resources applied with placeholder `"REPLACE_ME"` — real values deliberately deferred to Group 12 (see that group's checklist)
- [x] `/telco-churn/api-image`, `/telco-churn/ui-image` (String) — CD-mutated, `ignore_changes`, applied with placeholder `<repo-url>:unset`
- [x] `/telco-churn/rds-url-mlflow`, `/telco-churn/rds-url-app` (SecureString) — fully Terraform-derived, **two separate params, not one shared `rds-url`** (one Postgres URI can't name two databases). `rds-url-mlflow` won't actually connect until Group 6's deferred `CREATE DATABASE mlflow;` runs — that's tracked there, not here
- [x] `/telco-churn/s3-bucket` (String) — Terraform-derived
- [x] `/telco-churn/domain` (String) — static, human-picked (`telco-churn.duckdns.org`)
- [x] `refresh-env.sh` written (`infra/deploy/refresh-env.sh`): `get-parameters-by-path --with-decryption --recursive` → `/opt/telco-churn/.env` (chmod 600); the two `rds-url-*` params render as `MLFLOW_TRACKING_URI` / `POSTGRES_URL`. **Not yet deployed or run** — it ships to the box via Group 8's `aws_s3_object` upload + user-data, and needs `aws-cli`/`jq` installed there first
- [x] Confirm `--with-decryption` succeeds under the instance role — **verified 2026-09-18**, unblocked now that Group 8's EC2 box exists. **Real bug found and fixed:** `aws ssm get-parameters-by-path --path /telco-churn --with-decryption --recursive` from inside an SSM session on the instance (`arn:...assumed-role/telco-churn-ec2-instance/i-0c774c432d3cfbb23`) first came back `AccessDeniedException` on `ssm:GetParametersByPath`, even though `terraform plan` showed zero drift on `aws_iam_role_policy.ec2_instance` — the live policy already matched `iam.tf` exactly. Root cause: `ssm:GetParametersByPath` evaluates IAM against the *queried path itself* (`/telco-churn` → resource `parameter/telco-churn`, no trailing slash), and `iam.tf`'s `local.ssm_param_arn` was `.../parameter/telco-churn/*` — a slash before the wildcard, which requires a literal `/` after `telco-churn` and so never matches the bare path. `GetParameter`/`GetParameters` never hit this because those check full parameter names (`telco-churn/api-key`), which do contain the slash. **Fix:** dropped the slash — `.../parameter/telco-churn*` — matching AWS's own documented pattern for path-scoped access (`infra/iam.tf`'s `SsmParams` statement). `terraform apply` picked up only that one `Resource` string change plus the two already-known RDS drift items (`apply_immediately`, `rds.force_ssl` apply_method). Retest returned all 9 params with real decrypted `Value`s.

## Group 8 — EC2 + first boot

*OS: Ubuntu 24.04 LTS, not Amazon Linux 2023 — SSM Agent ships pre-installed either way; Ubuntu wins on tooling familiarity and portability.*

- [x] `data.aws_ssm_parameter` for the latest Ubuntu 24.04 AMI (`infra/compute.tf`)
- [x] `aws_instance`: `t3.micro`, public subnet, ec2 SG, instance profile, **no `key_name`** (SSM-only shell access), `http_tokens = "required"`, 20 GB gp3 root
  - **Incident, 2026-09-16: the box got destroyed and recreated three times chasing two separate ForceNew footguns before this stabilized.** (1) `data.aws_ssm_parameter.ubuntu_ami` tracks Canonical's "current" build, not a pinned ID — `ami` is ForceNew on `aws_instance`, so any apply after Canonical published a newer build force-replaced the instance. (2) `associate_public_ip_address = false` (added to dodge a second billed public IPv4) is a known AWS-provider footgun on instances in a subnet with `map_public_ip_on_launch = true` — AWS doesn't reliably report it back, so Terraform proposed replacing the instance on **every single apply**, not just once. **Fix:** `lifecycle { ignore_changes = [ami, associate_public_ip_address] }`, and the `associate_public_ip_address` argument removed from config entirely (left Computed) — accepting the ~$3.65/mo redundant public IPv4 as the price of the box not being destroyed on every future apply. Confirmed stable with a follow-up `terraform plan` showing `0 to add, 0 to destroy`.
- [x] `aws_eip_association`
- [x] DuckDNS record pointed at the Elastic IP; confirmed with `nslookup telco-churn.duckdns.org` → `100.59.128.63` (2026-09-16)
- [x] `compose.prod.yml`/`refresh-env.sh`/`Caddyfile` written to `infra/deploy/` and uploaded as `aws_s3_object` resources (`infra/compute.tf`) to the Group 4 bucket's `deploy-config/` prefix. **`compose.prod.yml`/`Caddyfile` are first-draft, pulled forward from Group 9 only because `aws_s3_object` needs real files to upload** — Group 9's own items (image-size review, `docker/api/Dockerfile` alembic files, UI `X-API-Key` header fix) are still open against them.
  - **Real gap found writing this — fixed 2026-09-16:** `configs/serving/api.yaml`'s `auth.enabled` now resolves via `${oc.decode:${oc.env:AUTH_ENABLED,false}}`; `compose.prod.yml`'s `api` service sets `AUTH_ENABLED: "true"`. No `serving/app.py` change needed — its existing `bool(cfg.serving.auth.enabled)` read sites already handle a real bool correctly.
- [x] User-data (`infra/deploy/user-data.sh.tpl`): installs Docker + compose plugin (Docker's own `apt` repo) + AWS CLI v2 + CloudWatch agent (host-level `mem_used_percent`/`disk_used_percent` only — Group 12 extends this); creates `/opt/telco-churn`; `aws s3 cp .../deploy-config/ ... --recursive`; installs the `telco-churn.service` systemd unit (`ExecStartPre=refresh-env.sh`, `ExecStart=docker compose -f compose.prod.yml up -d`) and `enable`s it — **deliberately not `enable --now`**, since `api-image`/`ui-image` are still Group 7's `:unset` placeholders and `docker compose up -d` would fail with no image to pull. Starts on every future boot once enabled by hand post-Group-9.
- [x] `terraform apply`; connected via Session Manager — re-verified on the final, stable instance (`i-0c774c432d3cfbb23`, post-ForceNew-fix, 2026-09-16) after the two earlier verified instances got replaced out from under this checklist item: Docker (`active (running)`), CloudWatch agent (`active (running)`), all three deploy-config files present in `/opt/telco-churn`, `telco-churn.service` `enabled`/`inactive (dead)` as expected

## Group 9 — Serving stack (`compose.prod.yml`)

- [x] `compose.prod.yml`: `api`, `ui`, `caddy`, `mlflow`; `restart: unless-stopped`; `api`/`ui` images via `${API_IMAGE}`/`${UI_IMAGE}` — **no `build:` block for those two**, images come from `cd.yml` (Group 11). `mlflow` is the one exception with a real `build:` block (same `docker/mlflow/Dockerfile` local dev uses) — it's a viewer-only service with no CD pipeline of its own.
- [x] **Reviewer-facing MLflow UI, scoped 2026-09-18 — reverses the earlier "no MLflow server container" decision.** Added the `mlflow` service: `docker/mlflow/Dockerfile` + `boto3` added to it. Command: `--backend-store-uri ${MLFLOW_TRACKING_URI}`, `--artifacts-destination s3://${S3_BUCKET}/telco-churn-training --serve-artifacts`, `--static-prefix /mlflow`. **Docker/mlflow/Dockerfile has no CD pipeline and isn't git-cloned onto the box, so it needed a delivery path of its own — added as a 4th `aws_s3_object` entry in `infra/compute.tf`'s `deploy_config_files` map (key `docker/mlflow/Dockerfile`, preserving the subpath so `aws s3 cp --recursive` lands it at `/opt/telco-churn/docker/mlflow/Dockerfile`), matching `compose.prod.yml`'s `build: { context: ./docker/mlflow }`.** Verified via real container build + boot: `boto3`/`psycopg2` importable, `--static-prefix /mlflow` genuinely moves every route under that prefix (confirmed `/mlflow/health` → 200, bare `/health` → 404 once static-prefix is set) — **this caught a real bug: the healthcheck was written against bare `/health`, which 404s under `--static-prefix`; fixed to `/mlflow/health`.**
- [x] `Caddyfile`: added `reverse_proxy /mlflow* mlflow:5000` gated behind `basic_auth` (bcrypt hash inline, demo password `telco-reviewer-2026` — publish alongside the API demo key per Group 13's bullet). Validated with the real `caddy validate` binary (`caddy:2-alpine`) — adapts cleanly to JSON.
- [x] **RAM budget bump applied to `infra/compute.tf`**: `aws_instance.main.instance_type` → `t3.small` (2GB), with the cost-impact comment (~+$7.59/mo) kept in the file. **Not yet `terraform apply`'d — that and everything else touching real AWS state is the user's own step** (sandboxed Bash here has no AWS credentials/SSM tunnel access, consistent with this project's established two-terminal workflow); run `terraform plan`/`apply` from your own terminal when ready.
- [x] Verify IAM: confirmed by reading `infra/iam.tf` — `MlflowArtifactObjects`/`MlflowArtifactBucketList` statements already grant `s3:GetObject`/`s3:ListBucket` on the bucket to the EC2 instance role, and the network path to RDS is already open (mlflow runs on the same box/SG as api, and the `rds` SG already allows 5432 from the `ec2` SG). No IAM change needed.
- [x] Confirmed `mlflow` service's healthcheck mirrors local dev's (`/health` + `start_period: 90s`) — **adjusted to `/mlflow/health`, see the static-prefix bug above; local dev's own healthcheck is unaffected since it has no static-prefix.**
- [x] Logging: `awslogs` driver added for the new `mlflow` service too (api/ui/caddy already had it).
- [x] `api`: `MLFLOW_TRACKING_URI` + `POSTGRES_URL` (from Group 7's two params, via `env_file: [.env]`), `auth.enabled=true` (`AUTH_ENABLED: "true"`), `API_KEY` from env (same `.env`), healthcheck `/ready` — all already present, no change needed.
- [x] **Image-size / dependency-footprint review — resolved by doing the mlflow-skinny + pyarrow-extras swap (decided 2026-09-18, tested against a real throwaway local Postgres + real `docker build`, not assumed):**
  - `pyproject.toml`: `mlflow` moved out of base `dependencies` into the `training` extra (full mlflow — `mlflow ui`/`mlflow server` local dev still needs Flask/gunicorn); `serving`/`ui` extras gained `mlflow-skinny` instead. `pyarrow` moved from base deps into `training` (confirmed via grep: no parquet/pyarrow usage anywhere under `serving/` or `ui/`, including the specific function api/ui actually call in `models/artifacts.py` — `load_dev_oof_predictions` is the only parquet reader there, and neither image calls it). `psycopg2-binary` added to base `dependencies` (removed from the now-redundant dev group entry) — **a real, independently-necessary fix, not just cleanup**: MLflow's tracking client resolves the plain `postgresql://` `MLFLOW_TRACKING_URI` to SQLAlchemy's default *sync* driver, and `serving`'s only Postgres driver was `asyncpg` (async, used by `utils/db.py`'s own customer-lookup engine) — without this, the built `api`/`ui` images would have failed to connect to MLflow tracking at all, a gap nothing had caught yet because the only end-to-end Postgres-backed MLflow test so far ran from the dev `uv sync --all-extras` environment (which already had `psycopg2-binary` via the dev group), never from the actual serving image.
  - **Verified, not assumed:** a local throwaway Postgres container + `uv run --isolated --with mlflow-skinny ...` probe confirmed `SqlAlchemyStore` creation/migration, `mlflow.sklearn.log_model`/`load_model` with this project's own `cloudpickle` `serialization_format`, `mlflow.pyfunc.load_model`/`get_model_dependencies`, and `mlflow.artifacts.load_dict` all work under skinny + this project's own base deps, no extra packages needed. `uv lock` resolves cleanly (full `mlflow` in `training` and `mlflow-skinny` in `serving`/`ui` coexist fine — full mlflow itself depends on a matching mlflow-skinny). `mypy --strict` passes on `serving/`, `ui/`, `models/artifacts.py`, `models/environment_parity.py`. Both images actually built via `docker build` and their imports (`predict.py`, `models/artifacts.py`, `models/environment_parity.py`, `ui/streamlit_app.py`) run clean inside the container.
  - **Real measured sizes, same-lockfile-baseline comparison (not the stale 2026-09-16 figures, which predate `boto3` even being a base dep):** `ui` 989MB → 905MB (84MB saved, confirmed via a true before/after build off the same `uv.lock` state). `api` built at 875MB post-swap (no same-lockfile before-figure taken, but the same swap applies).
  - **Done 2026-09-18, after all: split `models/artifacts.py` by weight.** `resolve_champion_version`/`resolve_challenger_version` (+ their shared `_resolve_alias_version` helper) extracted into a new `src/telco_churn/models/registry_alias.py`, importing only `mlflow`/`mlflow.tracking`/`mlflow.exceptions.MlflowException`/`omegaconf` — no `mlflow.sklearn`, `pandas`, or `sklearn`. `models/artifacts.py` keeps only the genuinely heavy loaders (`load_fitted_model`, `unfitted_pipeline_from_manifest`, `load_dev_oof_predictions`, ...). **Honest caveat: import-time/code-quality win, not a disk-size win** — `scikit-learn` stays an unconditional base dependency either way (other paths need it), so this doesn't change the measured `ui`/`api` sizes above; it just stops `ui/streamlit_app.py` (which only ever needs the alias lookup) from paying `mlflow.sklearn`'s import cost to reach it. Five consumers re-pointed (`ui/streamlit_app.py`, `serving/predict.py`, `models/evaluate.py`, `models/register.py`, `pipelines/performance_check.py`); `dvc.yaml`'s `evaluate` stage (the only DVC stage that imports it) gained the new file in `deps:`; `tests/unit/test_artifacts.py` renamed to `tests/unit/test_registry_alias.py`. **Caught a real pre-existing bug in the process:** `tests/streamlit/test_streamlit_app.py`'s `_stub_champion_resolution` fixture was monkeypatching `telco_churn.models.artifacts.resolve_champion_version` (the source module), but `streamlit_app.py`'s `from ... import resolve_champion_version` binds a separate name in its own namespace — the patch was never actually reaching the real call site in `_resolve_champion_version_cached`. Fixed to patch `telco_churn.ui.streamlit_app.resolve_champion_version` instead. All 30 `test_architecture.py` guards, `mypy --strict`, `ruff`, and the full affected test set (140 tests: `test_registry_alias.py`, `test_predict.py`, `test_evaluate.py`, `test_register.py`, `test_streamlit_app_helpers.py`, `tests/streamlit/test_streamlit_app.py`) pass.
- [x] `docker/api/Dockerfile` gains `alembic.ini` + `alembic/`, copied into **both** the builder stage (so `uv sync` sees them, though nothing in `uv sync` itself needs them) and the runtime stage (so `docker compose run --rm api alembic upgrade head`, per Group 11's CD sequence, actually has migration files to run against). `alembic/env.py` already reads `POSTGRES_URL` from the environment — no further wiring needed.
- [x] `ui`: applied the `_api_request` `X-API-Key` header fix — reads `API_KEY` from env (already present via `compose.prod.yml`'s `env_file: [.env]`) and attaches it as `X-API-Key` on every request, matching `serving/app.py::require_api_key`'s exact header name/alias. Two new unit tests added (`tests/unit/test_streamlit_app_helpers.py`): header present when `API_KEY` is set, absent when it isn't. Full `tests/unit/test_streamlit_app_helpers.py` suite (26 tests) passes.
- [x] Bring the stack up once by hand; confirm `@champion` resolves at boot and `/ready` goes `503 → 200`. **Needs the real EC2 box (SSM session) — not runnable from here; this was the user's own manual step for this group**, along with the `terraform apply` for the `t3.small` bump and the Terraform-uploaded `docker/mlflow/Dockerfile`. **Completed 2026-09-19/20 — three real gaps found and fixed along the way, the last two much deeper than the first:**
  - **Incident 1 (2026-09-19):** `docker compose -f compose.prod.yml run --rm api alembic upgrade head` failed on its first-ever real pull — `pull access denied ... no basic auth credentials`. Root cause: the EC2 instance role's `ecr:GetAuthorizationToken`/`ecr:BatchGetImage` IAM *permission* (Group 2) was never wired up to anything that actually authenticates Docker itself — IAM permission to pull and being logged in are different things, and nothing in `user-data.sh.tpl` or the deploy sequence ever ran a `docker login` or configured a credential helper. Immediate unblock: a manual `aws ecr get-login-password | docker login` on the box (works via the instance role, no MFA) — but that token expires in 12h with nothing to refresh it, so every future pull (a reboot, or Group 11's CD) would hit the same wall once it goes stale.
    - **Real fix:** installed the Amazon ECR credential helper (`docker-credential-ecr-login` v0.12.0, downloaded + SHA256-verified from the official `amazon-ecr-credential-helper-releases` S3 bucket) in `user-data.sh.tpl`, configuring `/root/.docker/config.json`'s `credHelpers` for this account/region's ECR registry — Docker then transparently re-authenticates via the instance role on every pull, indefinitely, no login step ever needed. `infra/compute.tf`'s `templatefile()` call gained a new `account_id` var (`data.aws_caller_identity.current.account_id`, already declared in `iam.tf`) to render the registry hostname.
    - **Caveat that matters:** `user_data` changes don't retroactively apply to an already-booted instance — cloud-init only ever runs it once, at first boot, even after `terraform apply` updates the stored attribute (the same stop/modify/start cycle the `t3.small` bump used, but with no functional effect this time since cloud-init just sees "already ran for this instance-id" and skips it). The `user-data.sh.tpl` fix only benefits a *future* box (a replacement, or a fresh `terraform apply -replace`). The *currently running* box needed the credential-helper install applied by hand, once, via the SSM session — folded into `infra/scripts/manual-bring-up-on-box.sh` as its own step, and it also replaces the plaintext basic-auth token the manual `docker login` unblock left in `/root/.docker/config.json`.
  - **Incident 2 (2026-09-19/20): `mlflow` repeatedly exited `137` (SIGKILL) mid-startup, both alongside the other three services and later running completely alone with confirmed-healthy `free -h`/DNS — two-part root cause, only the second one actually stopped the crash loop.**
    - **Part A — worker fork storm at boot.** `sudo dmesg -T | grep -i "out of memory"` on the box showed real kernel OOM-killer entries naming `mlflow` specifically (e.g. `Out of memory: Killed process ... (mlflow) ... anon-rss:158836kB`), clustered right at container start. `mlflow server` with no `--workers` flag defaults to forking ~4 worker processes near-simultaneously at boot; on this box's 2GiB `t3.small`, that transient concurrent-fork spike was enough to trip the OOM killer before the app finished starting, even though steady-state single-process RSS is modest. **Fix:** added `--workers 1` to the `mlflow` service's `command:` in `infra/deploy/compose.prod.yml` — this is a low-traffic, reviewer-only admin UI with no real concurrency requirement. This fix alone was necessary but **not sufficient**: verified live, mlflow still died via OOM after surviving past this boot-time spike and passing two health checks over a minute in.
    - **Part B — the actual sustained cause: MLflow 3.14's server-side job-execution feature.** After Part A's fix, `dmesg` kept showing OOM kills alternating between `mlflow` and the `api` service's `uvicorn` process every 1-2 minutes, in a self-sustaining loop (each kill's restart caused a fresh transient re-init spike, tipping the *other* large process over next). `ps aux --sort=-%mem` on the box during a live window revealed the real cause: **7 separate `huey`-based background job-consumer processes** (`mlflow.server.jobs._huey_consumer`/`_periodic_tasks_consumer`, module paths under MLflow's internal async job-queue feature), each costing 169-203MB RSS — **~1.3GB combined**, on top of the mlflow server's own ~250MB. This is a documented upstream issue (`mlflow/mlflow#22792`, "Slow memory creep in MLflow tracking server when server-side GenAI jobs are enabled") — the feature exists for GenAI evaluation/deployment-job/webhook use cases this project has none of. **Fix:** `MLFLOW_SERVER_ENABLE_JOB_EXECUTION: "false"` added to the `mlflow` service's `environment:` block in `compose.prod.yml`.
    - **Verified, not assumed:** after the Part B fix, `ps aux` confirmed zero `huey_consumer` processes; `free -h` went from ~185Mi/1.9Gi free to ~965Mi/1.9Gi available. Stability confirmed via `docker inspect --format '{{.RestartCount}} {{.State.StartedAt}}'` on all four containers taken twice, ~16 minutes apart — identical restart counts and start timestamps both times (`mlflow`/`api` both showed zero restarts and unchanged `startedAt` across the window), proof against a real crash-and-quick-recover that a single point-in-time `docker compose ps` snapshot could miss.
    - **A 1GB swapfile was found already active on the box (`/swapfile`, `mkswap`'d, in `/etc/fstab`) from earlier troubleshooting** — a reasonable safety net independent of the two fixes above, kept in place. Notably, the alternating OOM loop was still happening *with* this swap already active and partially used, which is what pointed at a real, larger-than-swap-can-absorb memory hog (the huey processes) rather than the swapfile itself being an adequate fix on its own.
  - **Incident 3 (2026-09-20), minor, encountered mid-diagnosis:** interactive `aws ssm start-session` from Git Bash (MinTTY) repeatedly hung — stuck cursor, unresponsive `Ctrl+C`, multi-second lag per keystroke — both when the box was under real memory pressure (Incident 2) and, less severely, even once it wasn't. MinTTY doesn't give the Session Manager plugin a native Windows console, which is a known cause of exactly this. **Workaround used throughout the rest of this bring-up:** `aws ssm send-command`/`get-command-invocation` (fire-and-forget, no PTY) instead of an interactive session for every diagnostic and remediation step from this point on — more reliable than fighting the interactive session, and incidentally produces a clean, pastable command/response transcript. PowerShell as the interactive-session terminal is the other documented fix, not fully verified here since `send-command` covered everything needed.
  - **Phase F (verification curls, from RUNBOOK.md) — all passed 2026-09-20** against `https://telco-churn.duckdns.org`: `/health` → `200 {"status":"ok"}`; `/ready` → `200` with real `model_version`/`run_id`; `/predict` no key → `401`; `/predict` with key, empty payload → `422` (schema validation, key itself accepted); UI root → `200`; `/mlflow/` no auth → `401`, with `reviewer`/`telco-reviewer-2026` basic auth → `200`.
  - **Caddy switched from Let's Encrypt staging to production CA (2026-09-20):** `infra/deploy/Caddyfile`'s `acme_ca https://acme-staging-v02...` global block removed now that a real cert had already issued cleanly once against staging — re-synced + `--force-recreate caddy`, re-verified the full Phase F matrix a second time with **no `-k`** needed anywhere, confirming a real trusted production cert.
  - **Resolved, local-machine-only, no Group 11 impact:** `aws ssm get-parameter`/`describe-parameters` on `/telco-churn/api-key` from this laptop's Git Bash consistently returned `ParameterNotFound`/an empty list, even under credentials that `terraform plan`/`apply` (same shell, same moment) read successfully. Root cause found via `aws ... --debug`, grepped for the actual outgoing request body: Git Bash's MSYS2 layer auto-converts any command-line argument that looks like an absolute Unix path into a Windows one, so `--name /telco-churn/api-key` was silently rewritten to `--name C:/Program Files/Git/telco-churn/api-key` before the CLI ever saw it — AWS was correctly reporting *that* nonexistent name as not found, every time. Terraform never hit this since it reads the parameter name as an HCL string literal, never as a shell argument passed through MSYS's path-mangling. **Fix (this machine only):** prefix the call with `MSYS_NO_PATHCONV=1`, e.g. `MSYS_NO_PATHCONV=1 aws ssm get-parameter --name /telco-churn/api-key --with-decryption --query Parameter.Value --output text`. Confirmed no bearing on Group 11's CD pipeline — GitHub Actions runners are Linux, no MSYS layer involved.

## Group 10 — CI (`.github/workflows/ci.yml`)

*Base `unit`/`integration` jobs already exist — this group closes three gaps found reviewing against `.pre-commit-config.yaml`/`pyproject.toml`.*

- [x] Base jobs already built (`unit`, `integration`)
- [x] **Gap 1:** add `uv run pre-commit run --all-files` as its own step — `black`/`detect-secrets`/`nbstripout`/hygiene hooks currently only run if a contributor's local hooks are installed. Dry-run against a scratch copy of the working tree (`SKIP=mypy`) surfaced three failures that would have gone red on the first push: `alembic/` import order (6 files, `ruff --fix`; CI's `ruff check src/` never covered `alembic/`), a `detect-secrets` false positive on the public GitHub OIDC thumbprint in `infra/iam.tf` (inline `pragma: allowlist secret`), and `end-of-file-fixer` on the untracked scratch file `orphan_run_ids.txt` (**not committed — decide whether to delete or gitignore it**). All other hooks pass. Not yet run on GitHub itself
- [x] **Gap 2:** SHA-pin `actions/checkout`, `actions/setup-python`, `astral-sh/setup-uv` (currently mutable version tags) — pinned all four, `.github/dependabot.yml` added (github-actions, weekly)
  - [x] Add `actions/cache` step for `~/.cache/pre-commit` (key: OS + Python version + `hashFiles('.pre-commit-config.yaml')`) before the `Pre-commit hooks` step, and SHA-pin it too
- [x] **Gap 3:** add a `docker build` (no push) step for both Dockerfiles — done as a separate `.github/workflows/docker-build.yml` (api/ui/mlflow matrix, path-filtered, PRs into `main` + pushes to `main`); `uv:latest` pinned to `0.11.16@sha256:440fd64…` in both Dockerfiles; Dependabot `docker` ecosystem added. All three images built locally with the pin. Smoke-test step added for `api`/`ui` (`/health`, `/_stcore/health`), verified locally: `ui` healthy in ~6s, `api` in ~16s **only** with `MLFLOW_TRACKING_URI=sqlite:////tmp/mlflow.db` — with no URI the container's default relative sqlite path is unwritable for the non-root user and `/health` never answers (not a prod issue, prod always sets the URI)
- [x] One-time: actually run `docker build` against both Dockerfiles locally right now — already covered: Group 9 built `api`/`ui`/`mlflow` for real (2026-09-18) and all three ran on the EC2 box (Phase F passed 2026-09-20). Current uncommitted Dockerfile diffs (`alembic` copies, `boto3`) are exactly those built-and-deployed changes; no rebuild needed
- [ ] Verify: a lint error *and* a black-only formatting violation both go red

## Group 11 — CD (`.github/workflows/cd.yml`, single-environment)

- [ ] **IAM prerequisite (written in `infra/iam.tf` 2026-09-20 as `ImageParams` — get+put on the two params only — plus `ssm:ListCommandInvocations`; needs your `terraform apply`, not yet applied),** easy to forget since Group 7 didn't need it:** add `ssm:PutParameter` on `/telco-churn/api-image` and `/telco-churn/ui-image` to `iam.tf`'s `ci_deploy` role policy — Group 7 only ever *read* SSM params under the instance role; nothing has granted `ci_deploy` write access to these two yet, and the "update the two `*-image` SSM params" step below will 403 without it
- [ ] `on: push: branches: [main]`; `permissions: id-token: write`; `concurrency: { group: deploy, cancel-in-progress: false }`. **Path-filtered (decided 2026-09-19):** scope the trigger to only what actually changes the built images —
  ```yaml
  on:
    push:
      branches: [main]
      paths:
        - "src/**"
        - "configs/**"
        - "alembic.ini"
        - "alembic/**"
        - "docker/api/Dockerfile"
        - "docker/ui/Dockerfile"
        - "pyproject.toml"
        - "uv.lock"
  ```
  Both Dockerfiles `COPY` `src/`, `configs/`, and (api only) `alembic.ini`/`alembic/` into the image, and both `uv sync --frozen` from `pyproject.toml`/`uv.lock` — a commit touching only `notebooks/`, `ANALYSIS.md`, `docs/`, or this TODO file produces a bit-for-bit identical image, so rebuilding+pushing a new SHA-tagged version for it is pure waste against `IMMUTABLE` ECR repos that already only keep the last 5 tags. Without this, `ci.yml`'s own `unit`/`integration` jobs still run on every push regardless (no path filter there — test coverage doesn't have this problem), only `cd.yml`'s build-and-deploy is scoped.
- [ ] **Written 2026-09-20, not yet run:** `.github/workflows/cd.yml` + `scripts/ssm_run.sh` (ships a script over SSM, polls, propagates exit code), `deploy_on_box.sh`, `prune_on_box.sh`. Needs repo variable `AWS_ACCOUNT_ID`; `aws-actions/*` are version-tagged, not SHA-pinned like the rest. Prune moved to *after* the smoke test. Steps: checkout → OIDC creds → ECR login → build+push `api`/`ui` tagged `${{ github.sha }}` → update the two `*-image` SSM params → `aws ssm send-command` running: `s3 sync` config → `refresh-env.sh` → `docker compose pull` → `docker compose run --rm api alembic upgrade head` → `up -d` → `docker image prune -f` → poll `get-command-invocation` → `scripts/smoke_test_serving.sh`
- [ ] **Decided 2026-09-20 — `mlflow` is built on the box, not in ECR; the deploy sequence's `up -d` needs `--build`.** `docker/mlflow/Dockerfile` has no ECR repo (only `api`/`ui` do) and `compose.prod.yml` builds it locally, so a plain `pull` → `up -d` would never pick up a changed Dockerfile (e.g. the `boto3` addition). Use `docker compose up -d --build` — near-instant on an unchanged layer cache; the only cost is a brief build spike on the 2GB `t3.small` (Group 9 OOM history) when the Dockerfile actually changed. Revisit a third ECR repo only if that spike causes trouble or `mlflow` starts carrying project code. `docker-build.yml` (Group 10) is the only pre-merge check of this Dockerfile.
- [ ] **Deploy-file delivery gap — closed 2026-09-20 (Phase 2 built directly, not deferred; not yet applied/run).** `cd.yml` now `aws s3 sync`s `infra/deploy/` (minus `RUNBOOK.md`/`user-data.sh.tpl`) + `docker/mlflow/Dockerfile` to `deploy-config/` before the SSM deploy; both paths added to its trigger filter (cost: a compose-only commit also rebuilds api/ui, spending one of ECR's 5 tags). `iam.tf` gains `DeployConfigUpload` (`s3:PutObject` on `deploy-config/*` only). **Terraform decision: kept `aws_s3_object` with `ignore_changes = [etag, source]`** — Terraform seeds, CD owns; dropping them would break a fresh environment's first boot (user-data needs the objects present). Needs your `terraform apply` (state-only for the `ignore_changes`).
- [ ] Written as **`scripts/smoke_test_deployed.sh`** (`scripts/smoke_test_serving.sh` is the existing local-compose check, left alone); runs *on the box* via SSM so the API key never reaches GitHub, `--resolve` pins the domain to loopback. Checks: `/health` 200, `/ready` 200, `/predict` no key → 401, `/predict` with key → valid probability, UI root loads
- [ ] **Rollback (built 2026-09-20, not yet exercised — supersedes the old "known gap" bullet, which was pulled forward from Phase 11):** `cd.yml` records the previous `*-image` values before flipping; if anything from the flip onward fails, it restores them, re-runs `deploy_on_box.sh`, re-smoke-tests, and stays red. The old premise ("layers already pruned") was wrong — ECR keeps the last 5 immutable tags, so the box just re-pulls. Rollback also restores the previous commit's deploy files to S3 (`scripts/sync_deploy_files.sh`, shared with the forward sync) — otherwise a broken `compose.prod.yml` would survive an image-only rollback; skipped on `workflow_dispatch`, which has no `before` commit. Limits: no rollback on the first-ever deploy (`:unset`); migrations are forward-only. **Still Phase 11:** the manual-approval gate. **Verify with a throwaway PR that deliberately breaks the *app* (e.g. make `/health` return 500 in `src/`), not the smoke script** — the rollback re-runs the same script from the same checkout, so a broken script would fail the rollback's own re-check too. Merge, confirm the box returns to the previous SHA and the job is red, then revert the PR. Documented in `infra/deploy/RUNBOOK.md`.
- [ ] Verify: a merge to `main` deploys and smoke-tests green, no laptop involved

## Group 12 — Operational monitoring + cost

- [ ] `aws_cloudwatch_log_group` per service (retention 14d); CloudWatch agent narrowed to host-level metrics only (`mem_used_percent`/`disk_used_percent` — logs go via `awslogs` driver instead)
- [ ] SNS topic + email subscription; alarms: `CPUUtilization > 90%`/15m, `StatusCheckFailed >= 1`, `mem_used_percent > 90`, `disk_used_percent > 90`
- [ ] **Before Alloy config: overwrite Group 7's two Grafana SSM placeholders** — `/telco-churn/grafana-endpoint` and `/telco-churn/grafana-api-key` are still `"REPLACE_ME"` from Group 7. Pull the real values from the Grafana Cloud console, then `aws ssm put-parameter --name <name> --type SecureString --value <value> --overwrite` for each
- [ ] Grafana Alloy on the box: scrape `api:8000/metrics` → `remote_write` to Grafana Cloud
- [ ] One Grafana dashboard: request rate, latency p50/p99, 5xx rate, prediction volume by route, `predicted_probability` histogram; alerts on p99, 5xx ratio, sustained `/ready` down
- [ ] Confirm `customerid` is not a label on any metric
- [ ] Teardown runbook ready (not run now — for when the 2027-01-16 deadline arrives): (1) `--no-deletion-protection` on the RDS instance, (2) `terraform destroy`, (3) `aws rds delete-db-snapshot` on the final snapshot in the same sitting (a real ~$1.90/mo charge Terraform never removes on its own)

## Group 13 — Doc ripples + changelog

- [ ] `docs/architecture.md` — "Deployment" section: chosen shape + App Runner/ECS-Fargate **cost comparison only** (no migration-path section — this project doesn't build it)
- [ ] `ANALYSIS.md §9` — two items: (1) single-instance/single-AZ/manual-patch limitations, (2) the Free Plan credit-cliff risk (access ends outright, not a gradual bill increase)
- [ ] **`ANALYSIS.md` §9 (or §0's economics section) — two `evaluate.py` diagnostic warnings surfaced 2026-09-18 during the five-step cycle's `dvc repro error_analysis` step (real backend, real numbers — worth writing up, not discarding as noise):**
  - `cost_scenario_spread_too_narrow`: `ev_spread=1992.03` across `configs/costs.yaml`'s cost scenarios vs. a `widest_within_scenario_ci_width=7361.00` — the scenario bracket is narrower than the bootstrap CI *within* a single scenario, meaning `configs/costs.yaml`'s scenario spread understates the real uncertainty in the retention rate `r`, which is the EV estimate's dominant error source per the pipeline's own hint.
  - `capacity_or_budget_exceeded` (both `base` and `optimistic` scenarios): at the shipped threshold, implied campaign spend (`$18,024`/`$27,899`) exceeds `campaign_budget=$15,000`, though contact count (266/207) stays under `contact_capacity=500`. Non-gating (§0: EV/economics never gate promotion) — but the pipeline's own hint says the correct policy response is top-K-by-EV contact selection (`economics.json`'s `ev_by_k`), not raising the threshold, which is worth writing into the narrative rather than leaving as a log line.
- [x] `README.md` — **done 2026-09-20.** Added a "Live demo" line up top (`telco-churn.duckdns.org`), fixed the Tech Stack table's "Cloud" row (was still "AWS ECR + App Runner + RDS + S3" — now "EC2 + Docker Compose, RDS, S3, ECR, Caddy"), reframed the Serving section's Docker Compose row as "local dev" and added a "Live deployment" row describing the AWS shape + the reviewer-facing MLflow UI (`/mlflow`, `reviewer`/`telco-reviewer-2026`), and added a Quick Start callout pointing to the live URL before the local-install steps — the README no longer reads as if `docker compose` on a laptop is the only way to experience the project. **Not done as part of this pass:** the Project Status table's phase sequencing (still shows a strict 10 → 11 → 12 order; the real sequence is 10a → 12a → 10b → 11 → 12b, per this file's own header and the actual git history) — left as a known follow-up, since fixing it properly means restructuring the whole table, a bigger job than this pass's scope.
- [x] **API demo-access decision — revised 2026-09-20, supersedes the 2026-09-16 note below.** Original plan was to publish a real, live `/telco-churn/api-key` value in `README.md` for `curl`ing the raw API. Reconsidered while writing it up: publishing the box's actual live key (not a separate throwaway) felt like the wrong tradeoff when the UI already demonstrates the full flow end-to-end. **Decided instead: no key published at all** — the UI is the demo, and `/docs` (Swagger) is the no-key way to see the schema and the `X-API-Key` requirement itself. **Real gap found executing this:** `/docs`/`/redoc`/`/openapi.json` were never actually routed to `api:8000` in `infra/deploy/Caddyfile` — with no explicit rule, they fell through to the catch-all `reverse_proxy ui:8501` and silently served the Streamlit UI instead of FastAPI's docs, contradicting the original 2026-09-16 decision below (which assumed `/docs` already worked). **Fixed:** added explicit `reverse_proxy /docs`, `/redoc`, `/openapi.json` rules to the Caddyfile, ahead of the catch-all. **Deployed and verified 2026-09-20** — a plain `curl -o /dev/null -w "%{http_code}"` alone wasn't trustworthy proof (Streamlit's catch-all can also return `200` for an unmatched path), so verification instead grepped the actual response body for content unique to FastAPI's real docs page: `curl -s https://telco-churn.duckdns.org/docs | grep -o "swagger-ui"` printed `swagger-ui`, confirming the route genuinely reaches `api:8000`, not Streamlit.
  - Original 2026-09-16 decision, for context: UI is the primary demo link, no key needed; leave FastAPI's `/docs` public so reviewers can see the schema/auth requirement without a key; publish a clearly-labeled "demo key" in `README.md` for anyone who wants to `curl` the raw API directly.
  - **Deferred idea, not scoped for 12a:** Swagger's "Try it out" is currently unusable without a key (correct — see above), which means a reviewer can see the schema but can't actually fire a request from `/docs` itself. If that's ever wanted, the safe way is a **second, dedicated demo-only API key**, distinct from the one the Streamlit UI uses internally — never publish the live operational key, since rotating a leaked/abused key would otherwise require a redeploy that briefly disrupts the UI's own working calls too. Implementing this is a real (small) code change, not just a config value: `serving/app.py`'s auth check currently validates against one configured key and would need to check membership in a small set of valid keys instead.
- [ ] `CLAUDE.md` — App Runner row/prose → EC2 + docker-compose (12a); App Runner/ECS-Fargate becomes a documented-not-built recommendation pointer to `README.md`
- [x] `PROJECT_PLAN.md`'s own tech-stack table — already fixed (2026-09-16)
- [ ] `CHANGELOG.md` — Phase 12a entry

---

## Final verification (before calling the phase done)

- [ ] `curl https://<domain>/predict` no key → `401`; with key → valid probability
- [ ] `/health`, `/ready`, `/metrics` all reachable with no key
- [ ] Streamlit UI loads and submits a prediction
- [ ] `@champion` resolves at boot against RDS+S3 — `api`/`ui` themselves run no MLflow server process (client-only, `mlflow-skinny`); the one `mlflow` server process on the box is the Group 9 reviewer-facing UI container, a deliberate 2026-09-18 addition, not a leftover
- [ ] `/ready` goes `503 → 200` on startup
- [ ] **Reboot the EC2 instance** — stack comes back with no manual step
- [ ] Close the laptop — service keeps serving
- [ ] Deliberate lint error fails `ci.yml`; a real merge to `main` deploys via `cd.yml` and smoke-tests green
- [ ] Grafana Cloud dashboard populates within minutes of driving traffic
- [ ] Monthly AWS cost confirmed at ~$28–30, tracked against the shared credit balance
